// Re-exported from shared/hooks — the implementation lives there so the payment
// machine (in shared/) can import it without an architecture boundary violation.
export { useNostrDirectMessage } from '@/shared/hooks/useNostrDirectMessage';
