diff --git a/node_modules/coco-cashu-core/dist/index.js b/node_modules/coco-cashu-core/dist/index.js
index c0b0a56..af710f7 100644
--- a/node_modules/coco-cashu-core/dist/index.js
+++ b/node_modules/coco-cashu-core/dist/index.js
@@ -139,6 +139,9 @@ var CounterService = class {
 		this.logger = logger;
 		this.eventBus = eventBus;
 	}
+	get counterService() {
+		return this;
+	}
 	async getCounter(mintUrl, keysetId) {
 		const counter = await this.counterRepo.getCounter(mintUrl, keysetId);
 		if (!counter) {
@@ -724,6 +727,9 @@ var ProofService = class {
 		this.logger = logger;
 		this.eventBus = eventBus;
 	}
+	get proofService() {
+		return this;
+	}
 	async createOutputsAndIncrementCounters(mintUrl, amount) {
 		if (!mintUrl || mintUrl.trim().length === 0) throw new ProofValidationError("mintUrl is required");
 		if (!Number.isFinite(amount.keep) || !Number.isFinite(amount.send) || amount.keep < 0 || amount.send < 0) return {
@@ -1823,6 +1829,9 @@ var MeltQuoteService = class {
 		this.eventBus = eventBus;
 		this.logger = logger;
 	}
+	get meltQuoteService() {
+		return this;
+	}
 	async createMeltQuote(mintUrl, invoice) {
 		if (!mintUrl || !mintUrl.trim()) {
 			this.logger?.warn("Invalid parameter: mintUrl is required for createMeltQuote");
