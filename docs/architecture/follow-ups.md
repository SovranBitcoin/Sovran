# Follow-ups

Open work carried over from the 2026-09-10 convention audit (formerly the
follow-up register in `SYSTEM.md`, now replaced by the rules in
[hunch.config.ts](../../hunch.config.ts)). "Confirmed" means the source behavior
was inspected, not that a production incident was reproduced. Fix one boundary
at a time: characterize the contract, change the owner and its callers together,
add the regression, then remove the item here.

| ID | Priority | Work | Done when |
| --- | --- | --- | --- |
| F02 | High, confirmed | Private message plaintext persists in AsyncStorage at **three** sites, not the one this entry used to name. (a) `createPubkeyScopedCache.ts` backs both `nip04Cache.ts` (NIP-04 plaintext) and `giftWrapCache.ts` (NIP-17 `UnwrappedDM`) — fix the factory, not the call sites. (b) `app/features/bitchat/stores/bitchatDmMessages.ts` persists `byPeer[].content` for BLE 1:1 messages marked `isPrivate: true`. Make plaintext memory-only, or design encrypted storage with retention. Blobs already written (`nip04-cache:v1`, `nip04-cache-neg:v1`, `nip17-unwrap-cache:v1`, `nip17-unwrap-cache-neg:v1`, and the bitchat DM store) hold plaintext on disk, so the change must also erase them. | Safe key migration; no plaintext at rest at any of the three sites, including blobs written before the fix; offline and profile-isolation tests |
| F03 | High, review | `amountToNumber` exists **five** times with four different answers for bad input, and this entry named one of them. `app/shared/lib/cashu/amount.ts` (24 importers) and `wallet/src/amount.ts` return `NaN` for an unparseable string with a warn/debug log; `wallet/src/balance/breakdown.ts` and `wallet/src/history/timeline/context.ts` silently return `0`; `app/e2e/funded-runtime/payment-request-payer.ts` throws — the only one that refuses to invent a number. All five map `null` to `0`, collapsing unknown into zero. `toSafeSatAmount` separately truncates. Converge on one implementation that separates display coercion from validation, taking the e2e version's refusal as the model, and keep `amountToNumberOrUndefined` for the genuinely-unknown case. | One `amountToNumber`; unparseable and unsafe input cannot become `0` or `NaN` on a path that authorizes a spend; unknown distinguishable from zero; callers classified |
| F04 | High, confirmed | Raw exception text reaches user-facing surfaces across the app, not the three places this entry used to name. A repo-wide `hunch check --all --only "errors/raw-error-to-ui"` returns 68 candidates: ~45 in `app/features` and `app/shared`, 7 in `wallet/` (`screen-actions/defaultHandlers.ts`, `machine/effects.ts`), 10 in dev tooling (`app/codereview/log-doctor`, `app/e2e/viewer` — CLI only, not shipped, ignore) and 6 in the devMode-gated settings section (raw text is the point there, ignore). Verified examples: `ReceivePaymentRequestTab.tsx:156` interpolates `${error}` into empty-state copy; `sovranPaymentConfig.ts` puts `rawError.message` into `nfcSendFailedPopup`; `defaultHandlers.ts` sets `message: err.message` on a UI-bound field. Route them through `describeError(error, service)` or the payment copy catalog. Re-run the rule to enumerate the current set rather than trusting this list. | No raw upstream text on a shipped surface; `errors/raw-error-to-ui` returns only dev-tooling and devMode-gated hits; the error still reaches the log |
| F05 | High, review | `createMergeWithSchema` falls back to defaults on rejection, including for critical stores. | Corruption can't silently reset identity or funds state; unrelated preferences survive |
| F07 | Medium | No app-wide i18n yet. Chosen direction: one locale service, Lingui with build-time compiled ICU catalogs, `expo-localization` for device preferences, wallet copy integrated through its existing resolver. Verify versions and `Intl` support on Hermes first. | UI, errors, wallet and native copy follow one locale policy; pseudolocale, RTL and large-text checks pass |
| F08 | Medium, confirmed | `app/shared/lib/date.ts`: the `iso` style isn't ISO 8601; `chat-bubble` "Yesterday" uses 24/48-hour windows; the locale resolver treats `'en'` as system. | Serialization separate from display; calendar-day, DST, invalid-time and locale tests |
| F09 | Medium, confirmed | `apiClient` relaxes shared profile schemas locally. Reconcile with `@sovranbitcoin/schemas` and the producer. | No unexplained local wire divergence |
| F10 | Medium, confirmed | `imageCache` prefetch has no queue cap or caller cancellation. | Bounded queue; old-scope work discarded; boot-gate abandonment tested |
| F12 | Medium, confirmed | `app/scripts/vendor-marmot-ts.sh` resolves a missing source and records no revision or license. | Known revision, mandatory license, repeatable refresh |
| F13 | Medium, review | Facade rebuild disposal and profile snapshot flush ordering in `buildNostrDataLayer`. | Identity or tier changes leave no old listener or late write |
| F14 | Incremental | Static `StyleSheet.create` and inline styles remain; migrate touched screens through the styling ratchet. | Ratchet decreases without layout drift |
| F15 | Incremental | Asset provenance, cache retention, and per-surface image transitions (the 1 s default delays dense rows). | Source and license known; row images avoid long fades |
| F16 | Incremental | Accessibility, reduced-motion and release-mode performance scenarios on devices. | Device evidence for focus, text scaling and offscreen work |
| F18 | Medium, confirmed | `dev` and `dev:wda` call `app/scripts/dev.sh` and `start-wda.sh`, which are git-ignored. | Startup works from a fresh checkout |
| F19 | Incremental | Apply the naming rules to overloaded payment, profile, key and time names, one cluster at a time. | Callers, mocks and persisted/wire names accounted for |
| F20 | Tooling | Cleanup measurements skip `wallet/src` and `nostr/src` and can pass on missing detector output. | Scope covers each owner; missing output fails |
| F21 | Medium, confirmed | `truncateMiddle` in `strings.ts` can lengthen strings and uses three periods; `DetectedActionRow` has its own helper. Converge on compact (8+8) and detail (12+12) profiles with one `…`. | No lengthening at thresholds; copied and signed values byte-identical |
| F22 | High, coverage gap | Not every route, alias, gesture and modal exit has a native journey on both platforms. | Each route has a journey or an explicit capability gap |
| F23 | High, native validation gap | [Shared navigation headers](../review/header-contract.md), decision recorded in `app/docs/adr/2026-09-19-shared-navigation-headers.md`: iOS harness lacks an installable entitled dev client; Android stops at Terms acceptance before the header gallery. P2PK title space/insets were corrected, but disappearing body text was not reproduced. | Run `headers` suite on both platforms; inspect named screens, large text, reduced motion, long names and iOS modal presentation shifts; reproduce and resolve any remaining P2PK disappearance |
| F24 | High, confirmed | The NIP-46 pairing secret travels in navigation state: `+native-intent` rewrites a `nostrconnect://` URI into `/(signer-flow)/connect?uri=…`, and that URI embeds the bearer secret. Hold the validated URI in a runtime-only store and route with no param or an opaque nonce. | No pairing secret in route params or navigation history; cold-start and warm deep-link journeys pass on both platforms |
| F25 | Medium, confirmed | Seven send/receive routes take a JSON-encoded Colada entry (schema allows 64,000 characters) as a route param instead of an identifier. Keep the entry in the payment machine context or a runtime store keyed by id. | Route params are identifiers; back-navigation, re-entry and deep-link entry keep working |
| F26 | Medium, review | Receive and mint-quote sequencing, and history reconciliation by polling, live in `app/features/send/lib/sovranPaymentConfig.ts` (`createSovranExecuteReceive`, `createSovranExecuteMintQuote`); rebalance hop expansion lives in `useMintRebalanceOrchestrator`. Move them beside the `wallet/` operations they override, injecting logger and copy. | The app triggers one wallet action per user action; wallet tests cover the moved steps; step ids referenced by swap transactions unchanged |
| F27 | Medium, review | The payment-operations port (`executeMelt(mintUrl, meltTarget, amount, unit)` and siblings) and the NIP-46 grant stores (`setPeerDecryptGrant`, `grantSession`; 22 and 33 call sites) take look-alike positional strings. Move each to one named-field request, port and implementers together. | A transposed call fails to compile; every implementer, mock and caller updated in one change |
| F28 | Medium, native | The bitchat JS-to-native interface passes five adjacent strings, two of them private keys, positionally (`startBLE`, `sendBLEPrivateMessage`), and native event payloads are typed but not validated at runtime. Move to Expo `Record` arguments in Kotlin, Swift and TS together; the module is a copied `file:` dependency, so it needs `bun install` and native rebuilds. | Record arguments on both platforms; malformed native events dropped by schema; Nut Drop and mesh journeys pass on devices |
| F29 | High, review | SecureStore key blobs (cached derived keys, Cashu mnemonic, seed) are `JSON.parse(…) as {…}`; `parseOrSelfHeal` only heals on a throw, so a wrong-shaped blob is returned as valid. Shape validation must match every format ever written, because a parse failure deletes the key. `useMnemonic` also never sets its `error` state and its mount effect voids a read that can reject. | Schemas proven against historical blobs; a wrong shape heals without deleting a recoverable key; the hook reports a failed read |
| F30 | Medium, confirmed | Persisted fields hold Nostr seconds under millisecond-style names: `muteList.createdAt` (`feed-ignore-store`), `OwnProfileSnapshot`/history `createdAt`, `relayListStore.updatedAt`, `contactsUpdatedAt`, `EngagementRecord.updatedAt` (seconds beside sibling maps in ms), `WallpaperCatalogEntry.createdAt` (mirrored in `@sovranbitcoin/schemas`), and in-memory `RecentContact.timestamp`. Each now documents its unit. Rename only with a `version` bump, `migrate` and a regression test per store. | Names carry `Sec`; an old blob still loads in a test for each store |
| F31 | Medium, review | Nagg wire schemas in `nostr/src/schemas.ts` and `wallet/src/nostr-mint-enrichment.ts` leave event `id`/`pubkey` as plain strings and `content`/`tags` uncapped. The envelope parses `events` as a strict array, so tightening first needs per-item tolerance or one bad event drops the page. `ignoreStore` repeats a lowercase-only 64-hex regex inside a persisted schema. | Per-item tolerant arrays; hex ids and size caps on network input; shared id schemas in `ignoreStore` without rejecting stored blobs |
| F32 | Low, review | `MintCurrencyTabs` and the `SendScreen` methods container animate layout properties per frame; `MintAddScreen` picks its search header by `Platform.OS` because the glass bar has no slot for the URL validation state; `downloadedThemeRegistry` mutates imported theme constants in place; `PermissionGestureDemo` draws its own glove glyph because the icon registry stores every icon as 16x16. | Transform or layout-transition based motion checked on devices; validation state has a home on both platforms; theme owners expose register/unregister |
| F33 | Low, review | The melt-cancel handler in `wallet/src/screen-actions/defaultHandlers.ts` decides whether a rollback failure is ignorable by matching Coco's error message text, because Coco throws plain `Error` with no class or code. | Coco exposes a typed rollback error and the handler branches on it |
| F34 | Tooling | The 2026-09-21 Hunch audit finished partial: 2 failed requests, and `budget.maxRulesPerHunk` skipped rules on 2,938 chunks (mostly tests) although the committed budget is 1,024 rules per chunk. It also reported 2,607 findings for rules the config switches off, so the run did not use the committed config. The reworded rules in `hunch.lock` have not been re-scored because no model key with quota was available. | A complete `check --all` on the reworded rules; false-positive rate measured per rule; the off-rule reporting explained or fixed |
| F35 | Medium, confirmed | `clearUnitOverride` in `wallet/src/react/ColadaProvider.tsx` releases the machine's unit pin by VALUE, not by owner: the NearPay radar and its peer list both pin `'sat'`, so the list unmounting clears the radar's pin until the radar's wallet context next changes. NearPay now refuses to start on a non-`sat` real unit, which hides the effect today. | `setUnitOverride` returns a release function over a per-binder stack; a nested binder's unmount restores the previous pin; a machine test covers it |
| F36 | Medium, confirmed | NFC auto-pay reads `getContext()` right after `switchUnit` (`wallet/src/machine/createMachine.ts`, the payment-request unit switch). The new unit's balances only arrive once React re-runs the `usePaymentFlowMachine` binding effect, and the machine tests use a synchronous context, so a stale read would not be caught. | The switch awaits a context whose unit matches, or fails closed; a test with an asynchronous context provider covers it |
| F37 | Low, confirmed | A testnut Nut Drop is announced in its REAL unit: the receive toast and the radar celebration read "Received 1,000 sats" (`nutDropAutoRedeem.ts`, `useNutDropCelebration.ts`, `NutDropCelebrationOverlay.tsx`). History and balances are scoped correctly; only the announcement is not. The picker's `NOT_IN_PAYMENT_REQUEST` copy ("Not accepted by payment request") is also shown for a nearby recipient's allow-list, where there is no payment request. | Announcements use `toAccountUnit(unit, isTestnutMint(mintUrl))`; the allow-list reason names the recipient |
| F38 | Low, review | Mint audit copy still says "swap success" (`copy/src/site.ts`, `docs/reference/feature-inventory.md`) though the app now shows nagg's operations rate; the list pill rounds to a whole percent while mint info shows one decimal (99.5% reads "100%" beside "99.5%"); `MintReviewInfo.successRate`/`swapSuccess`/`swapTotal` in `wallet/src/types.ts` are unused; `nostr/src/facade/mint-reviews.ts` drops `uptime24h`, `avgLatencyMs`, `auditSource` and `auditUpdatedAt`. Upstream, nagg sends zero counts with `hasAudit:false`. | One rounding rule for both surfaces; copy names the measure shown; dead fields removed; nagg omits counts for unaudited mints |
| F39 | Medium, confirmed | The active-account rule reaches `resolveNext`'s pickers only by accident: a mint across the testnut split reads as `MINT_METHOD_UNSUPPORTED`, and flow pickers happen to hide that reason code (`hideMethodUnsupportedCandidates`). The two rules are independent — `accountMintCandidates` is applied explicitly in `requestMintSelector` but not on the `handleAmountEntered` → `resolveNext` path, whose no-requirement branch lists `walletCtx.trustedMintUrls` unfiltered. No reachable leak was found (outside-account mints also carry a zero balance), so nothing was changed; a characterization test in `wallet/__tests__/flows/mint-selector-account-scope.test.ts` pins the current behavior. | Every `selectMint` construction applies the account rule by name, not via a method-support reason code |
| F40 | Medium, partly fixed | Two windows in which the testnut account split is computed from verdicts the app does not have yet, so mint lists correct themselves after the fact. (a) `mintTestnutStore` is AsyncStorage-persisted but, unlike `themeStore` / `wallpaperStore` / `walletLifecycleStore`, tracks no `_hasHydrated`, and nothing gates on it — before it rehydrates every mint reads as real, so a `tsat` account momentarily has no mints at all. (b) A mint nagg has not probed has no verdict, and `isTestnutMint` answers `false` (real) until `refreshMintTestnutVerdicts` returns, which is a network round-trip, not a frame. Deliberately not fixed here: "unknown" cannot be answered synchronously, so the fix is a product decision (gate the picker, or show an explicit unverified state). Window (b) is now 10 minutes rather than 24 hours (`UNPROBED_RECHECK_MS`), after a captured session offered "as Onchain" for 1 sat through an unclassified testnut mint; window (a), and the fact that an unprobed mint still counts as REAL in between, remain. | The picker never renders rows derived from an unhydrated or unprobed verdict; a `tsat` account shows a loading state rather than an empty list |
| F41 | High, confirmed | The amount screen's Next menu ("Select options") is gated on `entry.methodContext` — a SNAPSHOT of trusted mints, balances and the per-unit capability map taken once by `createAmountEntryMethodContext` when `enterAmount` navigated. Nothing refreshes it: `AmountFlowScreen`, `AmountSelector` and `screen-actions/session` never touch `methodContext`, `decorateEntry` is skipped for `amountEntry`, and availability is computed by the manager from the raw entry. So funds arriving, a mint being trusted/untrusted, or a capability map rebuilt for a new unit while the screen is open do not re-gate the rails, and the menu can offer a rail whose picker then finds no mint. The unit half of this now fails closed (`getMintMethodCapability` rejects a capability derived for another unit) and the churn that caused it is gone (`resolveActiveUnit`), but the snapshot is still a snapshot. | The menu derives from the live wallet context, so a rail is offered iff the picker it opens has a candidate; a mid-flow balance or trust change re-gates it |
| F42 | High, confirmed | `app/shared/stores/profile/routstrStore.ts` persists `apiKey` to unencrypted AsyncStorage via `createProfileScopedStorage`, and `app/shared/lib/routstr/topUp.ts:45` can set that key to a raw Cashu token (`apiKey = encodedToken`). A Routstr key carries a prepaid balance, so this is bearer payment material at rest in plaintext — which `docs/review/contracts.md` explicitly forbids. Move it to SecureStore. | Key held in SecureStore; migration moves and erases the AsyncStorage copy; store still hydrates when the read fails rather than resetting |
| F43 | Medium, confirmed | `nostr/src/facade/relay/protocol.ts` opens a fresh WebSocket per relay per request (`openWebSocket` at :163 and :220) instead of keeping one connection per relay and multiplexing subscriptions over it, which NIP-01 asks for. The sibling Primal transport makes the same choice but records it — "One socket per request (simple + correct; a pooled multiplexed connection is a later optimization)" — and Primal's cache is not a relay, so only this file is a spec deviation. Its header documents the tier and the REQ/EVENT/EOSE wire but says nothing about socket lifecycle. Flagged by `nip01/single-connection-per-relay` at 0.80. | One connection per relay, subscriptions multiplexed by subscription id; reconnect and EOSE-quorum behaviour unchanged; fake-socket tests still cover the demux |
| F44 | Medium, confirmed | NUT-18 says a payment request's `t` array is sorted by preference, but transport choice ignores the order. `wallet/src/operations/defaultOperations.ts:1759-1760` does `find(t => t.type === 'nostr')` and `find(t => t.type === 'post')` independently, then `:1782` takes Nostr only when there is no HTTP transport — so a payee listing `[nostr, post]`, i.e. preferring Nostr, is paid over HTTP. `machine/transitions.ts:150` picks the same way. The cause is an implementation constraint, not a decision: the comment at :1790 notes coco's `PaymentRequestsApi` has no Nostr support, so HTTP is the path that already works. Flagged by `nut18/transport-shape-and-preference` (0.78, 0.72, 0.70). | Selection walks `info.transports` in order and takes the first supported entry; a request offering only one transport behaves as today; covered by a unit test over both orderings |
| F45 | Medium, confirmed | `nostr/src/schemas.ts` parses relay and Nagg responses with unbounded, unvalidated fields: `id` and `pubkey` are bare `z.string()` (:29, :31) rather than 64-hex, `content` is unbounded (:32), and `tags` is `z.array(z.array(z.string()))` with no cap (:33); `routes`, `capabilities` and profile `name` likewise. The same file bounds one array at `:22` (`.max(5000)`), so the intent exists but is applied in one place. `nostr/src` has no hex primitive of its own, and cannot import the app's `NostrPubkeyHexSchema` from `protocolIds.ts` because `nostr` must not import from `app`. Flagged by `conventions-zod/hex-ids` (13 findings) and `input-size-caps` (14) across `schemas.ts`, `envelope.ts`, `facade/mint-reviews.ts`, `facade/primal/schemas.ts` and `recipes/wallpapers.ts`. Caps are a policy decision on the untrusted-input boundary — too low rejects legitimate events — so pick them deliberately rather than by reflex. | A hex primitive in `nostr/src` used for every id and pubkey; every relay-facing string, array and record bounded; limits chosen against real event sizes and documented; oversized input rejected at parse, not held |
| F46 | Medium, confirmed | Five surfaces re-arm or refresh on navigation focus alone, with no `AppState` `'active'` handling, for things the OS tears down while backgrounded: `useAmbientNfcArm.ts` (NFC arming, 0.93), `useNostrTierHealth.ts`, `ShareSignerScreen.tsx`, `SignerRequestsScreen.tsx`, `UserMessagesScreen.tsx`. `useFocusEffect` fires on screen focus, not on app foreground, so a screen that stays focused across a background/return never re-runs it — the UI can report armed or subscribed while the underlying resource is gone. The repo already uses `AppState` in eight places, including `nip46Engine.ts` and `nip46Transport.ts`, so the signer internals handle this while the signer screens do not. There is **no shared helper**, which is why it is applied unevenly. Flagged by `conventions-react-native/focus-not-foreground`. | A `useForegroundEffect`-style helper owning focus + AppState together, used by all five; re-arm verified on a device across background and return, since neither the OS teardown nor expo-router's refocus behaviour can be settled by reading |
| F47 | Medium, confirmed | Async cancellation and single-flight discipline is uneven across shipping code. `conventions-async-tests` returns **61 findings outside tests and tooling**, clustered as: `signal-unchecked-after-await` (17) — an `AbortSignal` checked before an await but not after, so a superseded request still writes; `unbounded-fanout` (10) — `Promise.all` over a caller-sized list with no concurrency cap; `sleep-ignores-cancel` (9) — bare `await new Promise(r => setTimeout(r, …))` that no abort can interrupt, including `defaultOperations.ts:~2030` inside `executeAutoRedeem`'s reconciliation poll and three in `sovranPaymentConfig.ts`; `guard-released-on-every-path` (8) and `guard-before-await` (7) — single-flight guards whose release is not on every path, including `wallet/src/machine/createMachine.ts:1501`; plus `non-error-throwables` (6), `status-before-body` (6) and `url-unencoded-interpolation` (4). Individually small; together they are the reason a superseded search, a stale profile sync or an abandoned poll can still land. | Cancellation checked after every await on a path that writes state; fan-out bounded; sleeps take a signal; each single-flight guard released in a `finally`; the rule family returns only tooling and test hits |
| F48 | Medium, confirmed | Persisted collections grow without an entry cap in 13 stores — `ownedMediaStore` (`byBlob`, 0.96), `transactionLocationStore`, `swapTransactionsStore`, `transactionDistributionStore`, `transactionAnnotationStore`, `mintStore`, `themeStore`, `ctaStore`, `nostrSocialStore`, `wallpaperStore`, `nip46ConnectionsStore`, `dataMigrations`. They cap key length and inner arrays (`z.string().max(600)`, `.max(2000)`) but not the number of entries, so the blob grows linearly with use forever — slower hydration and larger AsyncStorage, though `tolerantRecord` means it degrades rather than failing parse. `routstrStore` already solved this: it trims in `partialize` against named ceilings, and its comment records why — 1,024 sessions was a schema `.max()` with no trimming, so session 1,025 failed parse and `createMergeWithSchema`, being all-or-nothing, discarded the entire store. The pattern exists and was not propagated. Flagged by `conventions-state/persist-unbounded-collection` (14). | Each persisted collection has an entry ceiling and a trim in `partialize`, with the eviction order chosen per store; ceilings shared by the schema and the trim as `routstrStore` does; existing oversized blobs trimmed on migration rather than rejected |
| F49 | Medium, confirmed | A genuine wallet 402 still dead-ends in a manual top-up. `useAiSend.ts` raises the "Insufficient balance" action menu and `navigateToTopUp` pushes the send-flow amount keypad, where the user guesses an amount with no relation to the shortfall the node just reported. The reference client instead deposits and retries: reactive top-up of the shortfall on a 402 (`routstr-sdk/client/RoutstrClient.ts:1032-1175`) plus a proactive one when `available < required × 1.4` (`:1969-2020`). Sovran cannot do either today because there is no headless mint-and-send — token creation is driven entirely by colada's amount-entry state machine and intercepted at `sovranPaymentConfig.ts:1513-1546` — so this is a payment-boundary change, not an AI-feature one. Deliberately deferred: the failure that prompted this work was an upstream 402 on a funded wallet, which auto top-up would not have fixed. Prefilling the keypad with the shortfall needs the same colada seam. | A wallet 402 funds itself from the local wallet and retries once, with the manual popup as the fallback when the wallet cannot cover it; `executeRoutstrTopUp` remains the only top-up path |
| F50 | Medium, confirmed | `sendMessage` (`app/shared/lib/routstr/api.ts`) passes only the caller's `AbortController` — it is the one Routstr call with no timeout. `getModels`, `checkBalance` and `topUpBalance` all pass `buildAbortSignal({ timeoutMs: ROUTSTR_TIMEOUT_MS })`; the chat request does not, and `useAiSend` creates a bare `new AbortController()` with no timer. A node that accepts the connection and never responds wedges the composer (`disabled={isSending}`) until the OS reaps the socket. Stream stalls are warn-only. Three test files mock `buildAbortSignal` to `undefined` and `aiSendRecovery` mocks `sendMessage` outright, so nothing exercises timeout behaviour anywhere. The constant's own comment (`api.ts:44-49`) says "match the streaming-side 60s budget" while the value is `30_000`. Note a streaming budget is not a request budget: the cap belongs on time-to-first-byte and on inter-chunk silence, not on total duration. | A connect deadline and an inter-chunk stall deadline on the chat request, both cancellable; the constant and its comment agree; a hung-node test that does not mock the signal builder |
| F51 | Medium, review | The streaming path is almost certainly dead on device. `parseSSEStream` (`api.ts:619-631`) uses `response.body.getReader()` when available and otherwise logs `routstr.sse.no_readable_stream` and falls back to `await response.text()`. React Native's `fetch` (whatwg-fetch) does not expose `response.body`, and no streaming polyfill — `react-native-fetch-api`, `textStreaming`, `expo/fetch` — is installed anywhere in the project. If so, every completion buffers whole and arrives at once: streaming, the typewriter haptics (`useAiSend.ts:475-478`), `ai.stream.first_token` and the "Thinking for N seconds" counter are all inert. Unproven only because no completion has ever succeeded in a captured session — confirm by looking for `routstr.sse.no_readable_stream` in the first successful one. | The device log shows chunks arriving incrementally, or the fallback is removed and a polyfill adopted deliberately; the ReadableStream branch is covered by a test that does not stub `response.body` |
| F52 | Low, confirmed | `app/log.txt` is a 74 MB device log committed to the repo. It is the best evidence in the Routstr 402 investigation (session `muezkuty-4877m8` is what proved the balance was funded), so it should be extracted rather than simply deleted — but it does not belong in git, and `app/` is inside the bundle root. | The capture lives outside the repo or in a trimmed excerpt under `app/docs/diagnostics/`; `log.txt` is gitignored; no device log ships in either Metro bundle |
| F53 | Medium, confirmed | A Routstr balance is per NODE — the key is `sk-<sha256(token)>`, a row in that node's own database — but `routstrStore` holds one `apiKey` and one `balance` with no node binding. When nagg repoints `node.baseUrl` (its ladder switches on catalog health alone, and the app follows), the stored key is unknown on the new node, the first request 401s, `throwResponseError` clears key and balance, and whatever was deposited stays on the old node reachable only through `POST /v1/wallet/refund` — which the app never calls and whose credential it has just deleted. Observed 2026-09-24: repointing off `ai.redsh1ft.com` stranded a 250-sat deposit. routstr-sdk solves this with a per-`baseUrl` key map plus `refundApiKey`/`refundProviders` (`wallet/BalanceManager.ts`), swept on a 42-minute timer — a timer being exactly what a suspended mobile process does not run, so the hooks here are app-foreground and node-change, not an interval. | Balance and key are stored per node; a node change refunds the old key before discarding it, or keeps it recoverable; a 401 on a repointed node cannot delete the only credential that could reclaim the funds |
