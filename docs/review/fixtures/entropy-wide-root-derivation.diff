# expect: 
# task: Review the changed behavior in this synthetic policy fixture.
diff --git a/app/shared/review-fixture.ts b/app/shared/review-fixture.ts
--- a/app/shared/review-fixture.ts
+++ b/app/shared/review-fixture.ts
@@ -1,3 +1,4 @@
 export function childKey(rootSeed: Uint8Array, index: number): Uint8Array {
-  return rootSeed;
+  // rootSeed is 32 bytes from crypto.getRandomValues; BIP-32 expands it by design.
+  return hkdf(sha256, rootSeed, undefined, new Uint8Array([index]), 32);
 }
