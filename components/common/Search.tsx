import React from 'react';
import { View } from 'react-native';

export default function SearchInput() {
  return (
    <View
      style={{
        flexDirection: 'row',
        alignItems: 'center',
        width: '100%',
        maxWidth: 400,
      }}></View>
  );
}
