# Sovran

[![Lint](https://github.com/SovranBitcoin/Sovran/actions/workflows/lint.yml/badge.svg)](https://github.com/SovranBitcoin/Sovran/actions/workflows/lint.yml)
[![Type Check](https://github.com/SovranBitcoin/Sovran/actions/workflows/type-check.yml/badge.svg)](https://github.com/SovranBitcoin/Sovran/actions/workflows/type-check.yml)
[![Knip](https://github.com/SovranBitcoin/Sovran/actions/workflows/knip.yml/badge.svg)](https://github.com/SovranBitcoin/Sovran/actions/workflows/knip.yml)
[![React Compiler](https://github.com/SovranBitcoin/Sovran/actions/workflows/react-compiler.yml/badge.svg)](https://github.com/SovranBitcoin/Sovran/actions/workflows/react-compiler.yml)
[![React Doctor](https://github.com/SovranBitcoin/Sovran/actions/workflows/react-doctor.yml/badge.svg)](https://github.com/SovranBitcoin/Sovran/actions/workflows/react-doctor.yml)

<!-- code: app.json; app.config.js; package.json; shared/lib/cashu/manager.ts; modules/bitchat-module/src/BitChatModule.ts; features/whitenoise/WhitenoiseProvider.tsx; shared/lib/routstr/api.ts -->

A Bitcoin wallet for Cashu ecash, Lightning, and offline payments. The current app is built with Expo and React Native, ships first on iOS, and combines Coco wallet state, Nostr identity, NFC tag handoffs, BitChat BLE mesh, Whitenoise MLS groups, and Routstr AI payments. Android configuration and fallbacks exist in the codebase, but the production target is still iOS-first.

On-chain Bitcoin inputs are recognized by the parser and by BIP-321 option ranking, but on-chain send and receive are not shipped yet.

## Contents

<!-- code: README.md -->

- [Current status](#current-status)
- [Protocol vocabulary](#protocol-vocabulary)
- [Feature inventory](#feature-inventory)
- [Technical shape](#technical-shape)
- [Platforms](#platforms)
- [Getting started](#getting-started)
- [Contributing](#contributing)
- [License](#license)

## Current status

<!-- code: app.json; app.config.js; eas.json; package.json; targets/widget/expo-target.config.js; plugins/withLowerPodDeploymentTarget.js; modules/liquid-glass-text/src/LiquidGlassText.tsx; modules/bitchat-module/src/BitChatModule.ts; features/map/screens/MapScreen.tsx; ../wallet/src/screen-actions/availability.ts -->

- **Primary target:** iOS app, minimum iOS 16.4.
- **Version in `app.json`:** 0.1.1.
- **Local native projects:** generated by Expo prebuild; `ios/` and `android/` are not checked in.
- **Android:** configured in Expo/EAS and guarded by platform fallbacks, but still a development target. Some surfaces need runtime configuration, such as `EXPO_PUBLIC_GOOGLE_MAPS_API_KEY` for Google Maps.
- **On-chain Bitcoin:** parsed as an address or a BIP-321 option, then marked unavailable with "Coming soon".
- **Adjacent protocol repos:** `../coco`, `../cashu-ts`, `../nuts`, `../nips`, `../luds`, and `../bips` are reference checkouts. There is no `../lips` checkout in this workspace; Lightning URL terminology comes from `../luds`.

## Protocol vocabulary

<!-- code: ../../nuts/00.md; ../../nuts/01.md; ../../nuts/02.md; ../../nuts/03.md; ../../nuts/04.md; ../../nuts/05.md; ../../nuts/10.md; ../../nuts/11.md; ../../nuts/13.md; ../../nuts/17.md; ../../nuts/18.md; ../../cashu-ts/src/model/PaymentRequest.ts; ../../luds/06.md; ../../luds/16.md; ../../nips/05.md; ../../nips/06.md; ../../nips/17.md; ../../nips/44.md; ../../nips/59.md; ../../bips/bip-0321.mediawiki; ../../coco/README.md; ../../cashu-ts/README.md -->

Sovran uses the names below in the same sense as the sibling protocol repositories.

| Term                      | Meaning in this app                                                                                                                                                            | Reference                                                        |
| ------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------- |
| Cashu proof               | Spendable ecash input generated from a mint's blind signature.                                                                                                                 | `../../nuts/00.md`                                               |
| Mint / melt / swap        | Minting receives ecash after paying a quote, melting pays a Lightning invoice with proofs, swapping rotates or changes proofs.                                                 | `../../nuts/03.md`, `../../nuts/04.md`, `../../nuts/05.md`       |
| Keyset                    | The mint's active signing keys for each amount and unit.                                                                                                                       | `../../nuts/01.md`, `../../nuts/02.md`                           |
| NUT-10 spending condition | A well-known `Proof.secret` format for extra spend requirements.                                                                                                               | `../../nuts/10.md`                                               |
| NUT-11 P2PK               | Pay-to-Public-Key locked ecash that requires a Schnorr signature from the recipient key to spend.                                                                              | `../../nuts/11.md`                                               |
| NUT-13 restore            | Deterministic secret/counter restore used by Coco-backed recovery flows.                                                                                                       | `../../nuts/13.md`                                               |
| NUT-17 subscriptions      | Mint WebSocket subscriptions used by Coco/NPC-style quote watching where supported.                                                                                            | `../../nuts/17.md`                                               |
| NUT-18 payment request    | Receiver-authored Cashu payment request, encoded as `creqA...` or `creqB...`.                                                                                                  | `../../nuts/18.md`, `../../cashu-ts/src/model/PaymentRequest.ts` |
| BOLT-11                   | Lightning invoice format used for mint quotes and melt quotes.                                                                                                                 | `@gandlaf21/bolt11-decode`, Lightning BOLTs                      |
| LNURL-pay                 | Static pay endpoint flow that returns a BOLT-11 invoice after amount selection.                                                                                                | `../../luds/06.md`                                               |
| Lightning address         | `name@domain` identifier that resolves to an LNURL-pay endpoint.                                                                                                               | `../../luds/16.md`                                               |
| NIP-05                    | DNS-based mapping from an internet identifier to a Nostr pubkey.                                                                                                               | `../../nips/05.md`                                               |
| NIP-06                    | BIP-39/BIP-32 derivation path `m/44'/1237'/<account>'/0/0` for Nostr keys.                                                                                                     | `../../nips/06.md`                                               |
| NIP-17 / NIP-44 / NIP-59  | Private DMs: unsigned rumors, NIP-44 v2 encryption, seals, and gift wraps.                                                                                                     | `../../nips/17.md`, `../../nips/44.md`, `../../nips/59.md`       |
| BIP-321                   | Modern `bitcoin:` URI replacement for BIP-21, including standard payment-instruction query parameters. Sovran also recognizes non-standard `cashu` hints inside the container. | `../../bips/bip-0321.mediawiki`                                  |
| Coco                      | Headless TypeScript Cashu wallet toolkit; Sovran uses `@cashu/coco-core`, `@cashu/coco-react`, and `@cashu/coco-expo-sqlite`.                                                  | `../../coco/README.md`                                           |
| cashu-ts                  | TypeScript Cashu protocol library used for low-level token/payment-request parsing and Cashu helpers.                                                                          | `../../cashu-ts/README.md`                                       |

## Feature inventory

<!-- code: features/; shared/; ../wallet/; modules/; targets/widget/ -->

_Reference — every capability Sovran ships today, organized by concern. Each item is either checked (`[x]` — shipping) or unchecked (`[ ]` — recognized in the code but not yet enabled)._

### Payment rails

<!-- code: ../wallet/src/parse.ts; ../wallet/src/detectors.ts; ../wallet/src/machine/createMachine.ts; ../wallet/src/operations/defaultOperations.ts; features/send/lib/sovranPaymentConfig.ts; shared/lib/cashu/manager.ts; shared/lib/cashu/npc.ts; shared/stores/profile/npcMintStore.ts; features/onboarding/screens/ClaimUsernameScreen.tsx; shared/ui/composed/QRCode.tsx -->

- [x] **Cashu ecash** — proofs, tokens, payment requests via [`@cashu/cashu-ts`](https://github.com/cashubtc/cashu-ts)
  - [x] **P2PK receive** — receive tokens whose proofs carry a NUT-10 well-known secret of kind `P2PK` (NUT-11) locked to your pubkey; unlocked automatically with the active profile's key
  - [x] **Cashu payment requests** — request-driven flow with amount, memo, and accepted mints
  - [x] **Animated UR QR codes** — multi-frame UR sequences via `@gandlaf21/bc-ur` for oversized payloads
- [x] **Coco wallet engine** — `@cashu/coco-core` + `@cashu/coco-expo-sqlite` + `@cashu/coco-react` for proof storage, swaps, and reactive state
- [x] **`colada`** — our own send/receive/mint-select state-machine library that sits on top of Coco
- [x] **Lightning**
  - [x] **BOLT-11** — invoice decoding via `@gandlaf21/bolt11-decode`, pay-to-invoice through mint melt quotes
  - [x] **Lightning address (LUD-16)** — pay any `name@domain.com` static internet identifier
  - [x] **LNURL-pay (LUD-06)** — `payRequest` flow over bech32 `lnurl1...` or `lightning:` URIs
- [x] **BIP-321** — the new `bitcoin:` URI scheme (supersedes BIP-21) for onchain + Lightning, extended in practice with a `cashu` parameter so a single URI can advertise all three rails; fallback rail UI when the chosen rail fails (the onchain rail is parsed and ranked but not yet sendable)
- [x] **Ecash send** — token-based Cashu sends through online recipient flows and offline handoff paths
  - [x] **Online ecash send** — `colada` drives amount, mint, recipient, and payment-request flows with mint checks before execution
  - [x] **Offline token handoff** — Coco can compose the token with no network once spendable proofs are selected
  - [x] **Share targets** — QR, system share, NFC, BLE mesh
  - [x] **Copy as text** — raw `cashuB...` token
  - [x] **Copy as emoji** — token encoded as emoji-only string for paste into emoji-tolerant apps (Twitter, iMessage)
  - [x] **Pending-ecash sweeper** — outgoing tokens the recipient never claimed stay live on the mint; the Transactions screen's Pending tab surfaces a "Cancel N pending" footer that mass-reclaims every visible row back into your wallet in one tap (with per-row swipe-to-cancel for single sends)
- [x] **NPubCash (NPC)** — receive to an `npub.cash` Lightning address through `coco-cashu-plugin-npc`; paid quotes sync into Coco using a persisted since-cursor. The v2 API returns paid mint-quote metadata rather than proofs, so the app mints each quote itself and dedupes on `(mintUrl, quoteId)`. The username screen offers free claims; npub.cash v2 has no availability endpoint, so names are validated locally and conflicts surface at claim time. Paid username claims are surfaced but not completed in that screen yet.

### Mint management & discovery

<!-- code: features/mint/screens/MintListScreen.tsx; features/mint/screens/MintAddScreen.tsx; features/mint/screens/MintDistributionScreen.tsx; features/mint/screens/MintInfoScreen.tsx; features/mint/screens/MintReviewsScreen.tsx; features/mint/screens/MintRebalancePlanScreen.tsx; features/mint/hooks/useAuditedMints.ts; features/mint/hooks/useMintCatalog.ts; features/mint/hooks/useNostrDiscoveredMints.ts; features/mint/hooks/useSovranDiscoveredMints.ts; features/mint/hooks/useMintRebalanceOrchestrator.ts; shared/lib/buildMintListItems.ts; shared/stores/global/auditMintStore.ts; shared/stores/global/mintInfoCache.ts; shared/stores/profile/mintStore.ts -->

- [x] **Multi-mint** — track balances across many mints simultaneously
- [x] **Mint rebalance** — orchestrated planner that moves proofs across mints with Split/Reset/Focus actions
- [x] **Auditor integration** — pulls mint uptime, swap success rate, and latency from the public auditor
- [x] **Auditor-driven mint discovery** — browse vetted mints by uptime and audit score
- [x] **Nostr mint reviews** — community reviews surfaced from Nostr alongside auditor data
- [x] **Mint state badges** — `OK` / `DEGRADED` / `DOWN` rolled up from recent swap history
- [x] **Mint info screen** — name, contact, TOS, audit score, swap stats
- [x] **Mint selector with dimming** — under-funded mints are dimmed inline rather than throwing
- [x] **Inactive-mint reclaim** — proofs from removed mints are reclaimable

### Nostr & identity

<!-- code: shared/lib/nostr/keyDerivation.ts; shared/lib/nostr/secureStorage.ts; shared/lib/nostr/nip17.ts; shared/lib/nostr/nip04Cache.ts; shared/lib/nostr/sendDirectMessage.ts; shared/providers/NostrKeysProvider.tsx; features/feed/screens/FeedScreen.tsx; features/feed/components/HomeFeed.tsx; features/feed/components/ThreadView.tsx; features/feed/components/nostr/NoteContent.tsx; features/payments/hooks/useContactSearch.ts; shared/ui/composed/ContactRow.tsx -->

- [x] **Key management**
  - [x] **NIP-06 multi-account derivation** — BIP-32/BIP-39 derivation at `m/44'/1237'/<account>'/0/0`; every Sovran profile derives its Nostr key from one mnemonic at a different account index
  - [x] **nsec import** — paste an existing `nsec1...` to bring an external Nostr identity into a Sovran profile (bypasses derivation for that profile)
  - [x] **Multi-profile isolation** — each profile has its own Nostr keys, BLE-mesh identity, and balances; switching remounts providers
- [x] **Identity resolution**
  - [x] **NIP-05** — map Nostr keys to DNS-based internet identifiers via `/.well-known/nostr.json?name=<local>`
  - [x] **NIP-05 on Lightning sends** — Lightning addresses (LUD-16) and NIP-05 identifiers share the same `local@domain` shape, so when you scan/paste a Lightning address Sovran also queries the domain's `/.well-known/nostr.json` and surfaces the linked Nostr profile, avatar, and Vertex credibility before you confirm
- [x] **DMs**
  - [x] **NIP-04** — legacy encrypted DMs with decrypt cache (`unrecommended` upstream; kept for backwards compatibility)
  - [x] **NIP-17 Private Direct Messages** — `kind 14` rumors sealed and gift-wrapped per NIP-59 (`kind 1059`), encrypted with NIP-44 v2
- [x] **Relays & search**
  - [x] **NDK Mobile** — relay management via [`@nostr-dev-kit/ndk-mobile`](https://github.com/nostr-dev-kit/ndk-mobile)
  - [x] **Nostr app-view** — Sovran-owned feed, profile, metrics, thread, notification, and DM-envelope queries served for fast app views. The default deployment is Nagg (self-hostable); the app queries it through the [`@sovranbitcoin/nagg-ts`](https://github.com/SovranBitcoin/nagg-ts) GraphQL client, with optimized REST app-view endpoints used where available (e.g. DM envelopes). nagg is zero-knowledge for DMs — it relays encrypted envelopes and never decrypts.
  - [x] **Vertex** — [Vertex](https://vertexlab.io) trust-ranked Nostr search and follower-graph reputation scores
  - [x] **Vertex credibility on rows** — reputation + followers shown inline on contact search and payment-recipient confirmations
- [x] **`nostr-tools`** — low-level signing, encoding, NIP utilities
- [x] **Profile switcher in drawer** — header avatar opens the drawer; drawer hosts the profile chrome
- [x] **Feed** (`kind 1` text notes per NIP-01)
  - [x] **Home timeline** with follow graph (`kind 3` contact lists)
  - [x] **Threads** with measured exit-shimmer skeletons while replies stream in
  - [x] **Image overlay** — tap an image in a note to open the fullscreen lightbox with zoom/pan
  - [x] **Video player** — inline Nostr video posts with autoplay and unmute on overlay
  - [x] **Reactions (NIP-25, `kind 7`)** — `+` likes and emoji reactions
  - [x] **Reposts (NIP-18, `kind 6`)** — share a `kind 1` text note to your own followers
  - [x] **Link parsing** — hyperlinks rendered with preview affordances
  - [x] **Image grid layout** — multi-image posts with adaptive grid
  - [x] **Ignore post / person** — mute a note or author from the thread view; the feed reactively hides ignored content
- [x] **Notifications & follows**
  - [x] **Notifications tab** — likes, reposts, replies, mentions, and zaps with All / Mentions / App filters and a policy menu
  - [x] **Follows & followers** — browse a profile's follow graph and open any entry
- [x] **Unified cross-surface search** — one search box on Wallet, Contacts, and Feed with scope tabs (All / Contacts / Feed / Posts), aggregated results, and recents on an empty query

### AI

<!-- code: features/ai/screens/AiChatScreen.tsx; features/ai/hooks/useAiSend.ts; features/ai/components/AiHeaderTitle.tsx; features/ai/components/ModelChip.tsx; features/ai/lib/format.ts; shared/lib/routstr/api.ts; shared/lib/routstr/topUp.ts; shared/stores/profile/routstrStore.ts; shared/stores/runtime/routstrTopUpStore.ts -->

- [x] **Routstr integration** — [Routstr](https://routstr.com) is a decentralized reverse proxy that fronts OpenAI-compatible LLM providers and bills per request in Cashu; Sovran uses your wallet as the funding source so there's no separate billing account
- [x] **Cashu-token top-up** — `colada` mints a Cashu token from your wallet and deposits it as Routstr balance; subsequent calls are paid out of that balance
- [x] **Model picker** — switch between models in chat
- [x] **Streaming responses** — token-by-token rendering
- [x] **AI chat as a tab** — first-class tab, not a settings page
- [x] **Pull-to-AI refresh** — pull-down gesture from the wallet surface to jump into AI (currently shipping)
- [x] **AI wallpaper backdrop** — tiled pattern wallpaper behind the chat surface

### BitChat (local BLE mesh)

<!-- code: modules/bitchat-module/src/BitChatModule.ts; modules/bitchat-module/ios/BitChatBLEBridge.swift; modules/bitchat-module/ios/BitChatNostrBridge.swift; modules/bitchat-module/ios/ProfileScopedBitchatKeychain.swift; features/bitchat/hooks/useBitChat.ts; features/bitchat/hooks/useBLEPeers.ts; features/bitchat/screens/GeohashChatScreen.tsx; features/user/screens/UserMessagesScreen.tsx; features/bitchat/stores/bitchatDmMessages.ts -->

- [x] **Native BitChat module** — local iOS `bitchat-module` bridging BLE mesh transport; Android exports degrade gracefully while the native bridge remains iOS-focused
- [x] **Private DMs over BLE** — peer-to-peer encrypted DMs with no relay, no internet
- [x] **Group chats over BLE** — multi-peer chats on the mesh
- [x] **Geohash chat rooms** — public location-bound chat rooms keyed by geohash precision
- [x] **Split-bill over BLE** — settle splits across nearby peers without any server in the path
- [x] **Nut Drop (ecash over BLE)** — pay a nearby BitChat peer in Cashu ecash from a honeycomb peer picker. The token is broadcast as a single **public** mesh message so any unmodified nearby receiver can reassemble the full token; the amount screen warns that anyone nearby can claim it. A private mesh-DM path is gated on longer mesh-DM payloads.
- [x] **BLE peer discovery in contacts** — nearby mesh peers appear in the contact list with mesh identity
- [x] **Delivery acks** — delivered / failed delivery state on DMs
- [x] **Profile-scoped BLE identity** — BLE peer identity is scoped to the active Sovran profile

### Whitenoise

<!-- code: features/whitenoise/WhitenoiseProvider.tsx; features/whitenoise/client/index.ts; features/whitenoise/client/network.ts; features/whitenoise/client/signer.ts; features/whitenoise/hooks/useWhitenoiseDM.ts; features/whitenoise/hooks/useWhitenoiseInbox.ts; features/whitenoise/hooks/useWhitenoiseRequests.ts; features/whitenoise/screens/WhitenoiseDMScreen.tsx; features/whitenoise/screens/WhitenoiseSetupScreen.tsx; features/whitenoise/components/WhitenoiseSetupBanner.tsx; shared/stores/global/settingsStore.ts; features/settings/screens/SettingsScreen.tsx; vendor/marmot-ts -->

- [x] **Whitenoise protocol** — MLS-based end-to-end encrypted Nostr group chat, powered by the vendored [`@internet-privacy/marmot-ts`](https://github.com/internet-privacy/marmot-ts)
  - [x] **MLS group chats** — forward-secret, post-compromise-secure ratcheting via the Marmot MLS implementation
  - [x] **Key packages** — publish your Whitenoise key package as a Nostr event so others can invite you
  - [x] **Invite-driven join** — accept or decline incoming group invites
  - [x] **Profile-scoped identity** — each Sovran profile has its own Whitenoise identity, isolated from the others
  - [x] **Dev-gated** — feature is hidden behind the developer toggle while it stabilizes

### NFC

<!-- code: shared/lib/nfc/index.ts; shared/lib/nfc/adapter.ts; shared/lib/nfc/write-token.ts; shared/lib/nfc/write.ts; shared/lib/nfc/ndef.ts; shared/lib/nfc/session.ts; shared/lib/nfc/apdu.ts; features/send/lib/sovranPaymentConfig.ts; features/send/screens/SendTokenScreen.tsx; app/(drawer)/(tabs)/index/_layout.tsx -->

- [x] **Tap modes**
  - [x] **Tap to read** — read a payment request, Cashu token, or other supported payload from an NFC Type 4 / IsoDep tag or terminal-like source
  - [x] **Tap to write a token to a tag** — write an ecash token to a blank NFC Type 4 / IsoDep tag
  - [x] **Tap to read a tag back** — receive a token by tapping a previously written tag
- [x] **Reliability**
  - [x] **Chunked writes** — oversized tokens span multiple records with a commit-only-on-success protocol so an interrupted write never bricks a tag
  - [x] **Auto proof reclaim** — if the tap drops mid-send, the proofs are reclaimed automatically
  - [x] **User-initiated dismiss is a no-op** — closing the sheet doesn't burn proofs
  - [x] **Steadier UX prompts** — "hold your device steady" instead of raw OS errors
- [x] **Routing** — NFC reads go through the same unified parser as paste/scan, so a tap drops you into the right flow (send, receive, mint add, contact open) automatically
- [x] **Bidirectional, online or offline** — every flow works without network

### Scanning & input

<!-- code: features/camera/screens/CameraScreen/CameraScreen.tsx; features/camera/screens/CameraScreen/CameraLayout.tsx; features/camera/hooks/useHandleCameraPermission.ts; features/camera/screens/StandaloneCameraScreen.tsx; ../wallet/src/parse.ts; ../wallet/src/normalize.ts; ../wallet/src/detectors.ts; ../wallet/src/machine/createMachine.ts; features/send/lib/sovranPaymentConfig.ts -->

- [x] **Unified parser** — one parser handles paste / scan / NFC / deeplink and ranks results so the most useful option surfaces first
- [x] **Recognized payloads**
  - [x] Cashu tokens — both `cashuA...` (v3, JSON) and `cashuB...` (v4, CBOR), including emoji-encoded variants
  - [x] Cashu payment requests (`creqA...` and `creqB...`)
  - [x] BOLT-11 Lightning invoices
  - [x] Lightning addresses (`name@domain`)
  - [x] LNURL (bech32 `lnurl1...`)
  - [x] BIP-21 / BIP-321 `bitcoin:` URIs (onchain + Lightning + ecash advertised together — onchain rail itself not yet sendable)
  - [x] On-chain Bitcoin addresses (legacy, P2SH, bech32, taproot) — recognized and parsed, but onchain send/receive is not yet shipped
  - [x] Mint URLs (`https://mint...`)
  - [x] NIP-19 bech32 entities (`npub`, `nprofile`, `nevent`, `note`, `naddr`)
  - [x] `nsec` for import flow
  - [x] Animated UR QR codes (`ur:bytes/...`)
- [x] **Gallery QR import** — scan a QR from a saved photo
- [x] **Camera permission gating** — explicit permission flow before camera access
- [x] **Flash control** — toggle torch in low light

### Deeplinks & URL schemes

<!-- code: app.json; app.config.js; app/_layout.tsx; ../wallet/src/react/ColadaProvider.tsx; ../wallet/src/normalize.ts; ../wallet/src/parse.ts; ../wallet/src/intent.ts; shared/lib/nav/routeSchemas.ts -->

- [x] **Custom schemes** — `bitcoin:`, `lightning:`, `lnurl:`, `cashu:`, `nostr:`, `web+nostr:`
- [x] **Sovran scheme** — `sovran://` for in-app routes
- [x] **Universal links** — `https://sovran.money/...` opens directly into the matching flow
- [x] **Cross-wallet compatibility** — broadened URI ingress to match Minibits and Macadamia handling so QR codes from those wallets just work
- [x] **`expo-linking` integration** — single ingress goes through the unified parser; no flow-specific parsing branches
- [x] **Modal-stacked profile opens** — scanned profile deeplinks open above any active modal flow instead of replacing it

### Discovery

<!-- code: features/map/screens/MapScreen.tsx; features/map/screens/MerchantDetailScreen.tsx; features/map/hooks/useMapMarkers.ts; features/map/hooks/useMapCamera.ts; shared/lib/map/mapClustering.ts; shared/lib/map/btcMapClusterCache.ts; shared/stores/global/btcMapStore.ts; features/mint/hooks/useAuditedMints.ts; features/mint/hooks/useNostrDiscoveredMints.ts -->

- [x] **btcmaps.org merchants** — Bitcoin-accepting merchants on an interactive map
- [x] **Map clustering** — `supercluster`-backed zoom-aware merchant clustering
- [x] **Location-aware focus** — center map on user via `expo-location`
- [x] **Search & filter merchants** — by name, payment method, proximity
- [x] **Auditor-driven mint discovery** — discover new mints via auditor's index
- [x] **Nostr-driven mint discovery** — discover mints via reviews and recommendations on Nostr

### Theme & visual

<!-- code: shared/lib/themeEngine.ts; shared/lib/colorExtraction.ts; shared/lib/wallpaperStorage.ts; shared/lib/wallpaperSync.ts; shared/providers/BackgroundProvider.tsx; shared/ui/capability/detect.ts; shared/ui/capability/defineVariants.tsx; shared/lib/version.ts; modules/liquid-glass-text/src/LiquidGlassText.tsx; shared/ui/composed/BackgroundView.tsx; shared/styles/tokens.ts; shared/lib/brandColors.ts -->

- [x] **Wallpaper-driven theming** — theme tints are derived from the user's chosen wallpaper via `react-native-image-colors`
- [x] **Nostr wallpaper sharing** — wallpapers are published and discovered through Nostr; download-then-apply so themes never flip before the asset is ready
- [x] **Hardened wallpaper pipeline** — remote thumbnail validation, per-URI palette fallback, and catalog/sprite/theme diagnostics with render-count + image-decode perf logs; the wallpaper background is memoized and skips the motion sensor on solid-colour themes
- [x] **Light & dark theme** — full light theme parity across chrome and content
- [x] **Liquid Glass** — `liquid-glass-text` native module for iOS 26 glassmorphism text
- [x] **Capability variants** — Liquid Glass / blur / mesh gradient gracefully fall back on older OS versions
- [x] **Device-radius scene corners** — drawer scenes adopt the device's actual screen corner radius via `expo-screen-corner-radius`
- [x] **Mesh gradients** — `expo-mesh-gradient` for animated backdrops
- [x] **Tile-pattern wallpaper** — AI tab background

### Wallet & balance

<!-- code: features/wallet/screens/WalletScreen.tsx; features/wallet/components/Account.tsx; features/wallet/components/PrimaryBalance.tsx; features/wallet/components/CurrencySwapperPill.tsx; features/wallet/hooks/useAppBalance.ts; features/send/screens/AmountSelector.tsx; features/transactions/screens/TransactionsScreen.tsx; features/transactions/components/Transactions.tsx; features/transactions/components/Transaction.tsx; features/transactions/components/TransactionIcon.tsx; features/transactions/components/TransactionLocationSection.tsx; shared/stores/profile/transactionDistributionStore.ts; shared/stores/profile/transactionLocationStore.ts; shared/stores/runtime/rollbackStore.ts -->

- [x] **Live BTC price** — real-time price feeds drive fiat equivalents app-wide
- [x] **Fiat-first amount entry** — type fiat, Sovran picks a composable sat amount from your proofs
- [x] **SAT / BTC / fiat unit switching** — pill swapper on the wallet face
- [x] **Per-mint balance breakdown** — see how proofs are distributed
- [x] **Transaction list** — sends, receives, swaps, splits — sorted, sectioned, with pending/confirmed groups
- [x] **Month-pager transactions** — swipe by calendar month through history
- [x] **Bulk pending-ecash sweep** — Pending tab footer reclaims every visible unclaimed outgoing token at once; per-row swipe-to-cancel for single targets
- [x] **Transaction source badges** — every row carries a small icon showing how the payment entered or left the wallet
  - [x] **Inbound** (scan history): `qr`, `nfc`, `paste`, `deeplink`
  - [x] **Outbound** (distribution): `copy`, `share`, `airdrop` (BLE/NFC/local), `displayed` (shown as QR on-screen)
  - [x] **BIP-321 option kinds** — for BIP-321 scans, the badge also exposes which rails the URI advertised (onchain / lightning / ecash)
- [x] **Transaction location stamps** — optional geolocation captured at the moment a transaction finalizes
  - [x] **Privacy-gated reveal** — locations stay hidden behind a placeholder and only render after explicit user reveal
  - [x] **Grayscale map** — rendered via `expo-maps` with a desaturation overlay so location is glanceable but not flashy
  - [x] **Per-flow capture** — visible on receive-token, Lightning/onchain receive, Lightning send, and send-token detail screens
  - [x] **Opt-out** — setting can be disabled globally; when off, nothing is captured or stored
- [x] **Transaction status indicators** — single `LoadingIndicator` block handles loading / success / error / reverted across the app

### Send & receive

<!-- code: features/send/providers/Colada.tsx; features/send/lib/sovranPaymentConfig.ts; features/send/lib/createSovranScreenActionsBridge.ts; features/send/screens/AmountFlowScreen.tsx; features/send/screens/AmountSelector.tsx; features/send/screens/LightningSendScreen.tsx; features/send/screens/OnchainSendScreen.tsx; features/send/screens/PaymentRequestScreen.tsx; features/send/screens/SendTokenScreen.tsx; features/receive/screens/ReceiveScreen.tsx; features/receive/screens/LightningReceiveScreen.tsx; features/receive/screens/OnchainReceiveScreen.tsx; features/receive/screens/ReceiveTokenScreen.tsx; ../wallet/src/machine/transitions.ts; ../wallet/src/screen-actions/availability.ts -->

- [x] **Send flow state machine** — `colada` machine drives amount → mint → recipient → execute
- [x] **Recipient identity enrichment** — NIP-05 + Nostr profile resolved before you confirm
- [x] **Mint revalidation** — mint health revalidated on amount entry
- [x] **Token detail share screen** — copy (text or emoji), share, NFC side-by-side
- [x] **Receive flow** — mint selection, BOLT-11 mint quote, live quote status
- [x] **Lightning melt** — pay an invoice from any mint that can cover it
- [x] **Multi-rail fallback** — BIP-321 rails dim on failure; remaining rails are offered inline

### Split bill

<!-- code: features/splitBill/hooks/useSplitBillOrchestrator.ts; features/splitBill/hooks/useSplitBillParticipantPicker.ts; features/splitBill/components/ParticipantCard.tsx; features/splitBill/components/ParticipantCardDeck.tsx; features/splitBill/components/ParticipantRow.tsx; features/splitBill/lib/candidateToIdentity.ts; features/splitBill/lib/reconcileSplitBillHistoryUpdate.ts; shared/stores/profile/splitBillTransactionsStore.ts; app/(split-bill-flow)/participants.tsx; app/(split-bill-flow)/detail.tsx; app/(split-bill-flow)/summary.tsx -->

- [x] **Participant picker** — pick from contacts, BLE mesh peers, or Nostr directory
- [x] **Per-person amount adjust** — split evenly or set custom amounts
- [x] **BLE-mesh settlement** — settle over BitChat with no relay
- [x] **NPC / Nostr settlement** — fall back to Lightning address or Nostr DM payment
- [x] **Settlement tracking** — per-participant paid/owed status

### Contacts

<!-- code: features/contacts/screens/ContactsScreen.tsx; features/contacts/hooks/useAllSearchResults.ts; features/contacts/lib/navigateToProfile.ts; features/payments/hooks/useContactSearch.ts; features/payments/hooks/useRecentContacts.ts; features/payments/hooks/useMintContacts.ts; shared/ui/composed/ContactRow.tsx; features/bitchat/hooks/useBLEPeers.ts; shared/stores/runtime/mockDataStore.ts -->

- [x] **Unified search** — name, NIP-05, npub, Lightning address all in one box
- [x] **Reputation + followers on rows** — Vertex-backed credibility surfaced inline
- [x] **BLE peers in contact list** — nearby mesh peers alongside Nostr contacts
- [x] **Mock contacts in demo mode** — for first-launch and screenshots

### Recovery & backup

<!-- code: features/settings/screens/SettingsRecoveryScreen.tsx; features/settings/screens/SettingsProfileScreen.tsx; features/settings/screens/DeleteScreen.tsx; shared/lib/nostr/secureStorage.ts; shared/lib/nostr/keyDerivation.ts; shared/providers/NostrKeysProvider.tsx; shared/lib/cashu/manager.ts; shared/providers/CocoProvider.tsx; shared/stores/global/walletLifecycleStore.ts; ../cashu-recovery; ../sovran-recover -->

- [x] **BIP-39 mnemonic** — single seed root for every profile's Nostr key (NIP-06) and wallet derivation
- [x] **Seed display & confirmation** — onboarding seed reveal + verification step
- [x] **Restore from seed** — re-derive all profiles from mnemonic
- [x] **`cashu-recovery`** — proof recovery tooling (sibling repo)
- [x] **`sovran-recover`** — Sovran-specific recovery flow (sibling repo)
- [x] **Secure storage** — `expo-secure-store` (no biometric gate on boot to avoid FaceID cascades)

### iOS widgets

<!-- code: targets/widget/widgets.swift; targets/widget/index.swift; targets/widget/expo-target.config.js; targets/widget/Info.plist; app.json; app.config.js -->

- [x] **Two branded widgets** — both ship in the same target via `@bacons/apple-targets` under `targets/widget/`
  - [x] **Bitcoin Pay** — Bitcoin ₿ logo on the Bitcoin-orange gradient
  - [x] **Sovran Pay** — Sovran S-mark on the same gradient
- [x] **Six supported widget families**
  - [x] **Home Screen**: `systemSmall`, `systemMedium`, `systemLarge`
  - [x] **Lock Screen / accessory**: `accessoryCircular`, `accessoryRectangular`, `accessoryInline`
- [x] **Tap-to-launch deeplink** — every size links to `sovran://camera?action=nfc-pay`, opening the NFC pay flow from the lock screen or home screen in one tap (the same deeplink can be repointed to any unified-parser route)
- [x] **Adaptive rendering** — orange gradient + white content on home-screen sizes; white background + black content on lock-screen sizes so iOS's vibrant cutout renders cleanly
- [x] **Pure SwiftUI Shape paths** — both logos are vector `Shape` paths drawn in SwiftUI (no bundled image assets); resolution-independent at every widget size
- [x] **iOS 16.4+** — uses `containerBackground` on iOS 17+ with a `ZStack` fallback for the iOS 16 minimum target

### Settings

<!-- code: features/settings/screens/SettingsScreen.tsx; features/settings/screens/SettingsProfileScreen.tsx; features/settings/screens/SettingsKeyringScreen.tsx; features/settings/screens/SettingsRecoveryScreen.tsx; features/settings/screens/SettingsRoutingScreen.tsx; features/settings/screens/SettingsStorageScreen.tsx; features/settings/screens/SettingsDesignSystemScreen.tsx; features/settings/screens/DeleteScreen.tsx; shared/stores/global/settingsStore.ts; shared/lib/cashu/manager.ts -->

- [x] **Profile management** — create, switch, delete profiles
- [x] **Keyring** — view nsec, derivation path, advanced key actions
- [x] **Mint list & rebalance entry** — manage active mints
- [x] **Fiat picker** — change display currency
- [x] **Theme settings** — wallpaper picker, light/dark override
- [x] **Developer toggles** — gate Whitenoise, mock contacts, and other dev-only surfaces
- [x] **Contact the Developer** — links to `@SovranBitcoin`

### Navigation & UX primitives

<!-- code: app/_layout.tsx; app/(drawer)/_layout.tsx; app/(drawer)/(tabs)/_layout.tsx; navigation/nativeTabs.tsx; shared/blocks/SovranTabBar.tsx; shared/ui/composed/Screen.tsx; shared/ui/composed/BottomButtons.tsx; shared/ui/primitives/Button.tsx; shared/ui/primitives/Pressable.tsx; shared/ui/primitives/SelectableCheck/index.tsx; shared/ui/composed/BackgroundView.tsx; shared/styles/tokens.ts; assets/icons/index.tsx; scripts/regenerate-icons.js; shared/lib/date.ts; shared/ui/composed/chat/ChatScreen.tsx -->

- [x] **Expo Router** — file-based routing under `app/`
- [x] **Modal flow stacks** — `(send-flow)`, `(receive-flow)`, `(mint-flow)`, `(split-bill-flow)`, `(settings-flow)`, `(theme-flow)`, etc.
- [x] **Drawer + bottom tabs** — drawer for profiles/settings, tabs for top-level surfaces
- [x] **`@gorhom/bottom-sheet`** — native bottom sheets for pickers and confirmations
- [x] **`heroui-native`** — in-card buttons, dialogs, segmented chips
- [x] **Project `Button` primitive** — hero CTAs in `BottomButtons` footers
- [x] **`SelectableCheck` primitive** — canonical selection checkmark
- [x] **Underline tabs** — replaced capsule tabs across the app
- [x] **`react-native-keyboard-controller`** — unified keyboard-lift math across AI tab and modal chats
- [x] **`@shopify/flash-list`** (v2) — high-performance lists across feed, DMs, and AI chat
- [x] **Iconify + internal SVG namespace** — `<Icon name="prefix:name" />` for 30+ icon sets plus an `internal:` namespace for brand glyphs
- [x] **Design tokens** — `shared/styles/tokens.ts` (spacing, radius, alpha, duration, zIndex, iconSize, hitSlop, shadow)
- [x] **Anchored color ramps** — semantic `success`/`danger`/`warning` flip with theme; no hardcoded hex
- [x] **Haptics** — `expo-haptics` on toggles and key actions
- [x] **Locale-aware dates** — `shared/lib/date.ts` (`formatDate` / `formatRelative`); in-app override → device locale → `'en'`

### Not yet shipped

<!-- code: ../wallet/src/screen-actions/availability.ts; ../wallet/src/screen-actions/defaultHandlers.ts; ../wallet/src/intent.ts; ../wallet/src/parse.ts; app.json; eas.json; modules/bitchat-module/src/BitChatModule.ts; features/map/screens/MapScreen.tsx -->

- [ ] **On-chain send/receive rail** — the parser recognizes addresses and BIP-321 onchain hints, but `availability.ts` marks the rail "Coming soon" and the send handler logs `onchainNotSupported`. No mint-quote-driven on-chain receive yet.
- [ ] **Production Android release** — Android app config, EAS scripts, JS fallbacks, and many Android-specific UI branches exist, but iOS remains the shipping target and some native features are iOS-only.

## Technical shape

<!-- code: package.json; bun.lock; tsconfig.json; eslint.config.js; jest.config.js; patches/; app/; features/; shared/; modules/; targets/widget/; ../wallet/; ../../skills/; AGENTS.md; CLAUDE.md -->

### Stack

<!-- code: package.json; bun.lock; tsconfig.json; eslint.config.js; jest.config.js; patches/; app.config.js -->

- [x] **Expo SDK 56** / **React Native 0.85.3** / **React 19.2.3** / **Hermes**
- [x] **Bun** as package manager and CI runtime
- [x] **TypeScript** with `noImplicitOverride`, type-aware ESLint rules, `eslint-plugin-react-perf`; React Compiler coverage is gated in CI by `react-compiler-healthcheck`
- [x] **Reanimated v4** (legacy `Animated` banned by lint)
- [x] **React Native Gesture Handler**, **Nitro Modules**, **Worklets**
- [x] **Zustand** with Zod-validated persistence
- [x] **Zod v4** schema validation; `@sovranbitcoin/schemas` for shared payloads
- [x] **`neverthrow`** for `Result` / `ResultAsync` return types
- [x] **`react-native-quick-crypto`** + `@noble/hashes` + `@scure/bip32` + `@scure/bip39`
- [x] **Uniwind + Tailwind variants** for styling
- [x] **`expo-sqlite`** for Coco wallet storage
- [x] **`expo-secure-store`** for seeds and nsec
- [x] **Apple Targets** (`@bacons/apple-targets`) for widgets and extensions under `targets/`
- [x] **Patch-package** for upstream Coco / cashu-ts modifications under `patches/`

### Repository map

<!-- code: app/; features/; shared/; ../wallet/; modules/; targets/widget/; patches/; ../../skills/; AGENTS.md -->

- `app/` — Expo Router routes and route-group stacks.
- `features/` — domain modules for wallet, send, receive, mint, transactions, feed, contacts, AI, map, split bill, settings, BitChat, Whitenoise, theme, and onboarding.
- `shared/` — cross-cutting UI primitives, providers, stores, Cashu/Nostr/NFC/Routstr helpers, theme and persistence infrastructure.
- `../wallet/` — in-repo workspace package (formerly `colada`) for the payment parser, state machine, guards, screen actions, LNURL/NIP-05 resolution, and tests.
- `../nostr/` — in-repo workspace package (formerly `nagg-ts`) for the Nostr app-view client and tiered data layer.
- `modules/` — local native modules: BitChat and Liquid Glass text.
- `targets/widget/` — iOS widget target managed by `@bacons/apple-targets`.
- `patches/` — patch-package patches for upstream dependencies consumed by the app.
- `../../skills/` and `AGENTS.md` — contributor/agent skills that govern architecture, UI, storage, security, and commit workflow.

## Platforms

<!-- code: app.json; app.config.js; eas.json; targets/widget/expo-target.config.js; targets/widget/widgets.swift; modules/bitchat-module/src/BitChatModule.ts; features/map/screens/MapScreen.tsx; shared/ui/capability/detect.ts -->

### iOS

<!-- code: app.json; app.config.js; eas.json; targets/widget/expo-target.config.js; targets/widget/widgets.swift; plugins/withLowerPodDeploymentTarget.js -->

- **App version in source:** 0.1.1
- **Local `app.json` build number:** 1
- **EAS build numbers:** remote auto-incremented by EAS (`appVersionSource: remote`)
- **Minimum OS:** 16.4
- **Bundle id:** `com.sovranbitcoin` in production, `com.sovranbitcoin.dev` for development/preview profiles
- **Widgets:** Bitcoin Pay and Sovran Pay under `targets/widget/`
- **App size:** 415 MB raw archive at upload, 94 MB bundled on TestFlight

### Android

<!-- code: app.json; app.config.js; eas.json; shared/ui/capability/detect.ts; shared/blocks/SovranTabBar.tsx; modules/bitchat-module/src/BitChatModule.ts; features/map/screens/MapScreen.tsx -->

- Expo/EAS config exists with package `com.sovranbitcoin`.
- Development and preview build scripts exist.
- Google Maps requires `EXPO_PUBLIC_GOOGLE_MAPS_API_KEY` or `GOOGLE_MAPS_API_KEY` at build time.
- Android uses flat/no-glass UI fallbacks and a JS tab bar; the BitChat native bridge is iOS-only in this checkout.

## Getting started

<!-- code: package.json; eas.json; scripts/dev.sh; scripts/start-wda.sh; scripts/vendor-marmot-ts.sh; modules/bitchat-module/scripts/patch-bitchat-imports.js -->

_How-to — fastest path to a running build._

**Prerequisites**

- [Bun](https://bun.sh) `1.3.5` (pinned via `packageManager` in `package.json`)
- Xcode 15+ with Command Line Tools and an iOS Simulator
- An EAS account if you want to make remote builds (`eas login`)

**Install & run locally**

```bash
bun install                  # installs deps + runs patch-package + bitchat patch
bun run start                # Expo dev server (Metro)
bun run ios                  # build and launch on iOS simulator
bun run android              # build and launch on Android when configured
```

**Other scripts**

- `bun run build:dev:ios` / `bun run build:dev:android` — EAS development builds
- `bun run build:ios` — EAS production iOS build (auto-submit)
- `bun run lint` / `bun run type-check` / `bun run test` / `bun run pretty:check` — checks
- `bun run analyze-structure` — structural health report
- `bun run audit` / `bun run fix` — Claude-driven audit and fix flows

## Contributing

<!-- code: ../../CLAUDE.md; ../../AGENTS.md; ../../skills/; codereview/audit.md; codereview/fix.md; codereview/analyze-structure/index.mjs -->

Sovran lives inside a workspace checkout of related repos (see [`CLAUDE.md`](../../CLAUDE.md) for the layout). Before opening a PR:

- Read [`CLAUDE.md`](../../CLAUDE.md) for workspace conventions (where to edit, which sibling repos are read-only, patch-package rules).
- Read [`AGENTS.md`](../../AGENTS.md) for the repo-local skill router.
- Load the relevant skills under [`../../skills`](../../skills) before writing code; Sovran-owned skills cover workspace ops, architecture, UI, data/runtime, security, payment-flow guards, compatibility, docs, and PR quality.
- Run `bun run lint && bun run type-check && bun run test` before committing.

## License

<!-- code: LICENSE -->

Sovran is licensed under the **Mozilla Public License 2.0**. See [`LICENSE`](./LICENSE) for the full text.

---

Last reviewed: 2026-06-06. Capability claims cross-checked against `../bips`, `../cashu-ts`, `../coco`, `../nips`, `../nuts`, `../luds`, and Routstr sibling repos on this date.
