# Testing coverage ledger (source-backed)

This is the coverage contract for Sovran's JSON-native test system. It maps the
executable payment contract in `wallet/docs/STATE_MACHINE.md`, adjacent app
integration seams, and the broader product surface to the cheapest credible test
layer. A green device run is not evidence for a row owned by Vitest or Jest, and a
green pure test is not evidence for rendered/native behavior.

The JS/JF/JP columns are intended ownership, not blanket execution claims. The
simulator lane requires explicit destructive-reset approval and a fresh owned
simulator per selected scenario. The funded lane has an approved fail-closed
runtime boundary and independently requires test-fund-loss acceptance; authored
scenario deferrals still short-circuit before side effects. Generic live and
physical execution remain disabled. JSON validation, planning, focused recovery
tests, and fake-driver orchestration provide no product proof.

The full-suite manifest must reference every authored `e2e/scenarios/*.json` file.
Unsupported work remains visible as `deferredReason`; it is never omitted or
silently treated as passing. `default.json` stays unfunded and safe.

For JF work, availability and evidence are separate. Selection needs both
`--i-approve-destructive-reset` and `--i-accept-test-fund-loss`, then an unlocked
current `~/.cocod` wallet through one pinned version-`0.0.16` binary. JSON may use
only typed semantic counterparty operations against declared exact assets.
The generated mnemonic crosses only authenticated random-token loopback IPC and
never console/Metro output; mnemonic/P2PK private material and resumable tokens
remain in mode-`0600` custody. Host `recovery.sweep` performs NUT-13
restore/send-all with no UI amount, and exact cocod before/after deltas must
reconcile. A global lock serializes funded runs, while stale custody must be
automatically resumed and re-audited before a dead lock can be cleared. These
contracts are implemented. Prepared app Cashu outflows retry only when wholly
`UNSPENT`, repair already-credited `SPENT` evidence idempotently, and retain
custody for `PENDING`, mixed, or partial/excess/negative balance-drift states;
only an exact full credit is attributable. Zero-credit `SPENT` value is terminal
only as explicitly accepted `spent-uncredited` loss. Raw screenshots use a
randomized `0700` scope and `0600` file, with pre/post AX safety geometry;
render-bound toast probes appear only after the real HeroUI components mount.
`send.cashu.sat`, `receive.lightning.sat`, `receive.cashu.paste`,
`receive.cashu.dismiss`, `receive.lightning.qr` (renamed 2026-07-14 from
`toast.receive-lightning`, whose relaunch/no-replay coda was removed),
`send.cashu.pending-reclaim`, `send.token.actions`, `send.lightning.sat`,
`send.lightning.dismiss`, `payment.request.delivery-rollback`, and
`tx.source.cashu-paste` have passing funded product evidence. Cashu Paste run
`2026-07-13T03-53-19-696Z-9c6fc229` proved controlled 50-sat token creation,
Paste redemption, finalized structured transaction truth, a +50 app balance,
exact host sweep, final app zero, and cocod `2882 → 2832 → 2882`
reconciliation. Dismiss run `2026-07-13T03-59-32-846Z-dcba992f` proved a
controlled 50-sat preview and prepared structured transaction, Cancel with zero
app balance or history mutation, host recovery of the unspent token, exact
reconciliation, final wallet zero, and owned-simulator deletion. Toast run
`2026-07-13T04-42-27-892Z-ec3f33af` proved the real rendered
processing→confirmed lifecycle, exact View routing to its incoming terminal
`ISSUED` transaction, +33 app balance, no replay after relaunch, raw QA
screenshots, exact host sweep/reconciliation, and owned-simulator deletion.
Pending-reclaim run `2026-07-13T05-14-34-351Z-a74ca7c0` proved a 40-sat pending
token from a funded 100-sat wallet, 60 sats / -40 delta, Check Status, cancel,
token-data removal, `rolledBack`, restored 100 sats / zero net delta, persisted
`Cancelled` / `Funds returned to your balance` detail, raw QA screenshots, exact
`2882 → 2782 → 2882` sweep/reconciliation, final wallet zero, and simulator
deletion. Token-actions run `2026-07-13T05-48-53-705Z-9486e246` proved exact
raw-text copy, emoji round-trip, current-iOS native share detection/dismissal, QR
speed Medium→Slow and density Heavy→Light, the rendered pending toast,
cancel/token-data removal, `rolledBack`, a restored 128-sat / zero-delta wallet,
and persistent returned-funds detail. It ended on the wallet, swept the app to
zero, reconciled cocod `2882 → 2754 → 2882` with Minibits unchanged at 1331
sats, and deleted the simulator. Lightning-send run
`2026-07-13T06-07-18-450Z-fa39bfff` proved the repaired bounded 100-sat funding
handoff and native-visible Paste selector, 40-sat invoice preview/payment,
product `PAID` truth before cocod settlement, an exact -40 app delta with no fee,
remaining-60-sat sweep, final-wallet reconciliation, exact cocod
`2882 → 2782 → 2822 → 2882`, unchanged 1331-sat Minibits balance, and
owned-simulator deletion. Lightning-dismiss run
`2026-07-13T06-42-23-795Z-ae2d2f7a` proved Cancel on a controlled synthetic
unpaid 40-sat preview with no Pay or settlement, a zero net wallet delta, no melt
history row, and raw preview/final-wallet screenshots. Recovery swept the exact
untouched 100 sats, reconciled cocod `2882 → 2782 → 2882`, and deleted the
owned simulator. Payment-request artifact
`run-2026-07-13T07-24-44-653Z-3cf0239f` passed in 329.8 seconds and proved a
deterministic 30-sat NUT-18 delivery failure: structured preview
`prepared`→`rolledBack`, rendered returned-funds failure, persisted `rolledBack`
detail, app `0 → 100 → 100 → 0`, raw unmasked screenshots, exact
`2882 → 2782 → 2882` sweep/reconciliation, zero fees, zero residual, final
wallet zero, and simulator deletion. Cashu source artifact
`run-2026-07-13T07-31-13-536Z-a311d9b7` passed in 256.8 seconds and proved an
incoming 50-sat Sovran-mint preview as `prepared` with no source, followed by a
distinct finalized transaction id with source `paste`. `Source` / `Clipboard`
rendered immediately and after reopening the wallet row; raw unmasked
screenshots captured every step. The app moved `0 → 50 → 0`, cleanup recovered
50 sats with zero residual and zero fees, cocod reconciled exactly
`2882 → 2832 → 2882`, the final wallet passed, and the fresh owned simulator
was deleted. Every other JF scenario remains unproved until its own authorized
run passes.

## Layer and status vocabulary

| Code | Layer            | Proves                                                                                                                                   |
| ---- | ---------------- | ---------------------------------------------------------------------------------------------------------------------------------------- |
| `WV` | wallet Vitest    | Parsers, legal transitions, state-machine effects, mint/unit/fee/proof math, rollback contracts, stale-generation behavior               |
| `AJ` | app Jest         | Stores, persisted schemas, route adapters, source/toast mapping, native payload parsing, deterministic component/accessibility contracts |
| `JS` | JSON simulator   | Intended owner for real navigation, sheets, keyboard/clipboard/deep-link boundaries, rendered UI, AX and screenshot evidence             |
| `JF` | JSON funded/live | Intended owner for controlled round-trips, balance conservation, terminal transaction truth, and reclaim/sweep                           |
| `JA` | JSON android     | Intended owner for REAL device-offline (airplane mode) via the android emulator driver — the `network` step + `device.network` capability |
| `JP` | JSON physical    | Intended owner for real NFC/BLE/NutDrop radio transport and two-device timing                                                            |

Statuses used below:

- `covered` — the cited Jest/Vitest test is present and owns this contract.
- `offline-ready` — JSON validates and plans; fake execution may exercise the
  harness, but no real product result has been recorded.
- `authored` — JSON steps exist, but the named scenario has not completed the
  required product run; no execution is claimed.
- `product-run` — the named simulator scenario passed on an owned ephemeral
  simulator; evidence applies only to that exact scenario and run.
- `deferred:<reason>` — intentionally short-circuits without touching a device.
- `gap` — intended product contract is not shipped; keep a red/contract test at
  the cheapest layer and do not author expected-success device automation.

## Stable requirement traceability

These IDs are the durable link between a product contract, an exact current test,
and the JSON plan or blocker. A cited pure test proves only its own layer.

| Requirement ID              | Exact current evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | JSON plan or explicit gap                                                                                                                                                                                                                                                                   |
| --------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `PAY-5.1-MINT-AMOUNT`       | `wallet/__tests__/flows/ecash-send.test.ts` table case `ECASH_SEND_CHANGE_MINT`; `app/__tests__/amountDraftStore.test.ts` test `returns and consumes the draft only when scope and unit match`                                                                                                                                                                                                                                                                                                                                                                 | `send.cashu.change-mint.amount`; product-run `2026-07-18T06-35-57-757Z-4280f8c6` proves the live two-mint amount-survival leg; the controlled-P2PK variant remains outstanding                                                                                                    |
| `PAY-5.1-P2PK-MINT`         | `wallet/__tests__/flows/ecash-send-p2pk.test.ts` test `keeps the lock through a mid-flow mint change`                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Same deferred plan; controlled P2PK injection and public `p2pk-lock-indicator` exist, while the mode-`0600` private key stays outside evidence. No product run is claimed                                                                                                                   |
| `P2PK-RECEIVE-ANNOTATION`   | `wallet/__tests__/unit/annotations.test.ts` test `reads counterparty, scan, swap, and p2pk`; `app/__tests__/sovranPaymentConfig.profile.test.ts` tests `skips P2PK key regeneration when the current-profile P2PK plugin is active` and `keeps regenerating P2PK keys after receive when no current-profile P2PK plugin key is active`; `app/__tests__/nutCreq.test.ts` test `round-trips mints + the P2PK lock key through a creq`                                                                                                                            | No JSON scenario yet. 2026-07-18 recon: the existing `controlledP2PKPubkey` plumbing serves only the SEND-lock/host-sweep direction; promotion additionally needs (1) a `lockPubkey` param on `cashu.create` + cocod locked-send capability detection (open question whether cocod 0.0.16 can lock), (2) a harness bridge exposing the app's ACTIVE receive P2PK pubkey (probe/state-mirror — never the private key), (3) a testID'd receive-side lock indicator + transaction-detail lock annotation (ReceiveTokenScreen's P2PK row is untestID'd; detail shell renders no lock), then receive/redeem, rotation (`YOUR KEYS (n)` count leg is proven), custody, and the ordinary post-swap sweep |
| `ONCHAIN-RECEIVE`           | `wallet/__tests__/flows/receive-method-mint-selector.test.ts` test `onchain scope routes the same lane with its own method`; `wallet/__tests__/screen-actions/defaultHandlers.test.ts` test `enters onchain receive from the default next handler (coco v2)`; `app/__tests__/mintQuoteScreenState.test.ts` tests `derives compact onchain confirmation progress for the timeline` and `encodes onchain mint quotes as BIP321 with the requested amount`; `app/__tests__/transactionDetailRoutes.test.ts` test `routes onchain mint entries to Onchain receive` | No live JSON claim. Promotion needs a controlled chain backend, deterministic confirmations/reorg handling, structured receive transaction fields and source annotation, balance conservation, exact mint/unit/account custody, and sweep                                                   |
| `ONCHAIN-SEND`              | `wallet/__tests__/unit/default-operations.test.ts` test `converts fiat-unit cents to sats for onchain amountSats`; `wallet/__tests__/screen-actions/availability.test.ts` test `offers onchain send and NOT Lightning for a scanned bitcoin address`; `app/__tests__/transactionDetailRoutes.test.ts` test `routes onchain melt entries to Onchain send`; `app/__tests__/onchainMeltTimelineState.test.ts` owns expiry/timeline presentation                                                                                                                   | No live JSON claim. Promotion needs the same controlled chain backend plus fee/quote bounds, broadcast/settlement truth, failure rollback/reclaim, structured send transaction fields and source annotation, exact custody, and sweep                                                       |
| `PAY-5.1-RECLAIM`           | `wallet/__tests__/screen-actions/defaultHandlers.test.ts` test `calls rollbackSend and notifies on success`                                                                                                                                                                                                                                                                                                                                                                                                                                                    | `send.cashu.pending-reclaim`; product-run `2026-07-13T05-14-34-351Z-a74ca7c0` proves 40-sat pending truth, Check Status, cancel/token-data removal, `rolledBack`, exact wallet conservation, persisted returned-funds detail, raw screenshots, sweep/reconciliation, and simulator deletion |
| `PAY-5.1-TOKEN-ACTIONS`     | `wallet/__tests__/screen-actions/availability.test.ts` test `surfaces emoji copy as a copy variant, not a sibling action`; `app/__tests__/menuActionFailures.test.tsx` owns action-menu failure handling                                                                                                                                                                                                                                                                                                                                                       | `send.token.actions`; product-run `2026-07-13T05-48-53-705Z-9486e246` proves text and emoji copy round-trips, current-iOS share detection/dismissal, QR controls, pending status, cancel/reclaim persistence, exact sweep/reconciliation, final wallet, and simulator deletion              |
| `PAY-5.2-FIXED-INVOICE`     | `wallet/__tests__/flows/lightning-melt.test.ts`; `wallet/__tests__/flows/manual-entry.test.ts`; `app/features/send/__tests__/SendScreen.destinationScan.test.tsx`                                                                                                                                                                                                                                                                                                                                                                                              | `send.lightning.sat`; product-run `2026-07-13T06-07-18-450Z-fa39bfff` proves bounded funding/Paste handoff, 40-sat preview/payment, product `PAID` before counterparty settlement, exact -40/no-fee app delta, 60-sat sweep, exact reconciliation, final wallet, and simulator deletion     |
| `PAY-5.2-CONFIRM-MINT`      | `wallet/__tests__/flows/lightning-melt.test.ts` test `allows mint change during flow`                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | `send.lightning.change-mint.preview`; product-run `2026-07-18T06-11-18-806Z-af4cb5d0` proves the confirmation-screen mint change with destination/amount survival and a single Minibits payment                                                                                                                           |
| `PAY-5.2-DISMISS`           | `wallet/__tests__/screen-actions/defaultHandlers.test.ts` test `calls rollbackMelt and notifies on success`; `app/__tests__/lightningSendDismiss.test.tsx` tests synthetic-preview exit without rollback and rollback-before-exit for real unpaid quotes                                                                                                                                                                                                                                                                                                       | `send.lightning.dismiss`; product-run `2026-07-13T06-42-23-795Z-ae2d2f7a` proves synthetic-preview Cancel, no Pay/settlement, zero wallet delta, no melt history row, raw screenshots, exact 100-sat sweep/reconciliation, and simulator deletion                                           |
| `PAY-5.3-MINT-AMOUNT`       | `wallet/__tests__/flows/mint-quote.test.ts` table case `MINT_QUOTE_CHANGE_MINT` and test `Next → "as Lightning" advances the machine to mintQuoteCreated`                                                                                                                                                                                                                                                                                                                                                                                                      | `receive.lightning.change-mint.amount`; product-run `2026-07-18T05-51-23-237Z-7be54b02` proves amount survival through the selector round trip, a fresh Minibits quote, live payment (+125), and sweep                                                                 |
| `PAY-5.4-DISMISS`           | Receive-token flow/parser tests own pre-redeem state; the named product run supplies the rendered dismissal and custody evidence                                                                                                                                                                                                                                                                                                                                                                                                                               | `receive.cashu.dismiss`; product-run `2026-07-13T03-59-32-846Z-dcba992f` proves controlled preview and prepared structured transaction, Cancel, zero balance/history mutation, host recovery of the unspent token, exact reconciliation, final wallet zero, and simulator deletion          |
| `PAY-5.5-DELIVERY-ROLLBACK` | `wallet/__tests__/unit/machine-effects.test.ts` test `returns a rolledBack result without treating rollback as a thrown failure`                                                                                                                                                                                                                                                                                                                                                                                                                               | `payment.request.delivery-rollback`; product-run `run-2026-07-13T07-24-44-653Z-3cf0239f` proves deterministic failure, `prepared`→`rolledBack`, rendered/persisted returned-funds truth, exact conservation/sweep, and deletion; automatic BIP-321 re-choice remains a gap                  |
| `LIFE-RECOVERY-REINSTALL`   | `app/__tests__/appGateReinstallDetection.test.tsx` test `flags a reinstall when a seed pre-exists without a seedCreatedAt marker`                                                                                                                                                                                                                                                                                                                                                                                                                              | `recovery.reinstall`; product-run proves same-simulator app reinstall, RestoreGate completion, exact drawer profile-name continuity, final wallet state, and cleanup                                                                                                                        |
| `LIFE-RECOVERY-FUNDED`      | Wallet restore/counter and app secure-storage tests own their isolated contracts; the host recovery tests own NUT-13 scanning, resumable custody, and exact reconciliation                                                                                                                                                                                                                                                                                                                                                                                     | No JSON scenario. Distinct app-data erase versus keychain-custody semantics plus rendered restored-proof/history truth remain gaps; the unexecutable deferred plan was deleted                                                                                                              |
| `NPC-RECEIVE-TOAST`         | `wallet/__tests__/flows/npc-mint-selector.test.ts` test `changes the NPC mint cleanly without any prior flow context`                                                                                                                                                                                                                                                                                                                                                                                                                                          | `receive.npc.sat`; complete deferred plan. The pinned cocod boundary probes `npc address`, toast/transaction seams and funded cleanup exist; live resolver behavior and NPC source annotation remain unproved/product gaps                                                                  |
| `UI-TOAST-REPLAY`           | `app/__tests__/paymentToastGating.test.ts` covers gating/failure transitions, including `flips the existing processing toast on failure instead of creating a new one`; it does not prove the rendered receive processing→confirmed→View→no-replay lifecycle                                                                                                                                                                                                                                                                                                   | `toast.receive-lightning`; product-run `2026-07-13T04-42-27-892Z-ec3f33af` proves real processing→confirmed timing, exact View routing to the incoming terminal `ISSUED` transaction, +33 balance, no replay, raw screenshots, sweep, and reconciliation                                    |
| `TX-SOURCE-PASTE`           | `wallet/__tests__/unit/annotations.test.ts` test `reads counterparty, scan, swap, and p2pk`; `wallet/__tests__/screen-actions/entry-matching.test.ts` test `matches by operationId when both are present`                                                                                                                                                                                                                                                                                                                                                      | `tx.source.cashu-paste`; product-run `run-2026-07-13T07-31-13-536Z-a311d9b7` proves source-less prepared preview → distinct finalized `paste` transaction, immediate/persisted Clipboard detail, +50 app delta, exact sweep/reconciliation, raw screenshots, final wallet, and deletion     |
| `SEC-PAYMENT-ROOTS`         | `app/__tests__/clearPaymentContext.test.ts` proves the clear helper resets Routstr, Nut Drop, contact, and amount-draft stores; `app/__tests__/paymentRootContext.test.ts` pins selected call sites and deep-link ownership                                                                                                                                                                                                                                                                                                                                    | `isolation.payment-roots`; draft requires redesign: dynamic back/close IDs, secret-free immediate probe, deterministic dirty-context/entry adapters, removal of remote Minibits, and a Routstr top-up root fix. Current later-root assertions can false-pass                                |
| `NFC-CONTEXT-ONLY`          | `wallet/__tests__/unit/machine-effects.test.ts` tests NFC creation/writeback, rollback/release on write failure, and stale-before-write; app NDEF/write-order tests cover pure adapters                                                                                                                                                                                                                                                                                                                                                                        | No runnable physical-NFC JSON scenario exists. A redesigned `isolation.payment-roots` can prove app context clearing only; it must never be reported as NFC radio/tag-loss/timeout/two-device transport evidence                                                                            |

## JSON scenario inventory (authoring status)

| Scenario                               | Layer | Status                                       | Unique evidence it is meant to prove                                                            |
| -------------------------------------- | ----- | -------------------------------------------- | ----------------------------------------------------------------------------------------------- |
| `onboarding.fresh`                     | JS    | product-run                                  | Splash, terms, all four carousel slides, Welcome checkpoint, wallet arrival                     |
| `onboarding.relaunch-interrupt`        | JS    | product-run                                  | Interruption + recovery axis on onboarding PROVEN LIVE (run `2026-07-18T19-34-40-030Z-7f622a05`): a fresh user accepts terms, reaches the intro carousel, then cold-relaunches (`reset:none`) before finishing — the app RESUMES at the carousel (asserted: intro slide visible, terms NOT re-shown, no blank/crash/stuck splash) and the interrupted user still completes onboarding through to the wallet |
| `onboarding.terms-gate`                | JS    | product-run                                  | Gating axis on onboarding PROVEN LIVE first try (run `2026-07-18T19-01-10-262Z-598fcbbd`): the Terms Next button is asserted DISABLED (AX state) before the acceptance checkbox is ticked and ENABLED after — a fresh user can't skip past terms; then the carousel completes to the wallet |
| `fault.mint-quote.create-offline`      | JS    | product-run                        | Mint-fault interceptor smoke PROVEN LIVE (run `2026-07-18T01-11-59`): quote POST killed pre-network, `mint-unreachable` popup observed via the new in-surface AmountSelector probe, ledger proof survives the goHome relaunch, ₿ 0 intact |
| `fault.melt.execute-offline`           | JF    | product-run                        | PROVEN LIVE (run `2026-07-18T02-54-37-466Z-1bdd0b00`): melt execute POST killed pre-network; coco executing-recovery verified UNPAID at the live mint before rollback (`rolled_back` + "Recovered: Swap happened but melt failed / never executed"), SEND CANCELLED timeline rendered, no popup fires on this path; final 18 (1-sat execute-swap fee is real), delta 0±1 |
| `fault.melt.execute-timeout`           | JF    | product-run                        | PROVEN LIVE (run `2026-07-18T03-01-14-818Z-d72d35c5`): melt execute hangs in-flight; rule-clear rejects the held fake, executing-recovery verifies UNPAID and restores exactly once; final 18 (execute-swap fee), delta 0±1 |
| `fault.melt.execute-500`               | JF    | product-run                        | PROVEN LIVE (run `2026-07-18T03-12-42-176Z-d3b49a78`): bare 500 on execute classifies like offline — executing-recovery verifies UNPAID at the live mint, SEND CANCELLED timeline, no blind restore; final 18 (execute-swap fee), delta 0±1 |
| `fault.melt.pending-hold`              | JF    | product-run                        | PROVEN LIVE (run `2026-07-18T03-31-33-487Z-f8d0fe16`): 20005 on execute with polls dead — proofs reserved through the 8s hold (no premature restore), single verified restore after clear; final 18 (execute-swap fee), delta 0±1 |
| `fault.melt.execute-20004`             | JF    | product-run                        | PROVEN LIVE (run `2026-07-18T03-38-04-927Z-290e94ef`): definitive 20004 lightning-payment-failed — cancelled timeline ('Funds returned…'), exactly one intercepted execute; final 18 (execute-swap fee), delta 0±1 |
| `fault.ecash-send.swap-offline`        | JF    | product-run                        | PROVEN LIVE (run `2026-07-18T04-17-52-686Z-68f9a47a`): whole mint offline during Create Ecash degrades to the offline proof-selector sheet via a real transport outage; dismissal debits nothing, balance 19 |
| `fault.ecash-send.swap-timeout`        | JF    | product-run                        | PROVEN LIVE (run `2026-07-18T04-24-07-963Z-13465e5c`): swap hangs (mint may have signed) — no half-state; rule-clear rejects the held fake and reconciliation lands balance exactly back at 19 |
| `fault.ecash-send.swap-10001`          | JF    | product-run                        | PROVEN LIVE (run `2026-07-18T04-30-10-369Z-0a70d8c4`): swap 400 {10001} — degrades to the offline proof-selector sheet (pinned via `e2e-action-menu-open`), single intercept (no retry), proofs untouched, balance 19 |
| `fault.ecash-send.swap-11001`          | JF    | product-run                        | PROVEN LIVE (run `2026-07-18T04-36-30-112Z-b036ea0b`): mint LIES proofs-already-spent — balance survives the unverified claim; coco recoverExecuting checks the REAL mint's /v1/checkstate (UNSPENT) and releases proofs; proof-selector sheet surface, balance 19 (`check:fault-fake-double-spend`) |
| `fault.ecash-receive.swap-offline`     | JF    | product-run                        | PROVEN LIVE (run `2026-07-18T05-13-52-886Z-6395f574`): fresh-₿0 wallet, trust-review granted live, second redeem hits the faulted swap — pending receive (network) recovers after clear and credits +10 gross / 9 net (testnut swap fee); single declared funding effect |
| `fault.mint-quote.issue-network-retry` | JF    | product-run                        | PROVEN LIVE (run `2026-07-18T05-31-07-326Z-b9bdfb4c`): real 100-sat Sovran receive with first TWO issuance POSTs killed — MintOperationProcessor backoff retries, third succeeds, credits exactly once (2 ledger intercepts); normal sweep |
| `fault.boot.all-mints-offline`         | JS    | product-run                        | PROVEN LIVE (run `2026-07-18T01-24-42`): cold boot with wildcard outage + pending quote — wallet interactive in budget, stores rehydrated, boot-time requests intercepted (boot-hang regression guard holds) |
| `fault.mint-add.unreachable`           | JS    | product-run                        | PROVEN LIVE (run `2026-07-18T01-30-13`): all three legs (offline/500/malformed info) fail without crash and persist no mint row |
| `fault.mint-switch.all-offline`        | JS    | product-run                        | PROVEN LIVE (run `2026-07-18T01-38-43`): preferred-mint switch completes instantly with every mint dead (user's seed case holds) |
| `fault.mint-quote.create-errors`       | JS    | product-run                        | PROVEN LIVE (run `2026-07-18T02-0x`): 500 → mint-unreachable, 20003 → general-error, 429 → general-error — the offline-vs-protocol classification contract holds at the UI |
| `fault.mint-quote.poll-timeout`        | JS    | product-run                        | PROVEN LIVE (run `2026-07-18T01-46-31`): polls hang in-flight, displayed invoice persists, clean dismissal after clear |
| `fault.boot.info-slow`                 | JS    | product-run                        | PROVEN LIVE (run `2026-07-18T01-51-32`): wallet interactive within 12s while /v1/info hangs 15s — boot never serializes behind mint metadata |
| `fault.melt.quote-errors`              | JF    | product-run                        | PROVEN LIVE (run `2026-07-18T04-10-32-022Z-e3c79f32`): quote offline AND 11006 both fail pre-debit via the `payment-status-melt-failed` toast on the synthetic preview (quote is only created on Pay; the static offline-vs-protocol popup classification is UNREACHABLE on the melt path — documented asymmetry vs mint-quote); balance 19, delta 0 both legs |
| `fault.melt.execute-20007`             | JF    | product-run                        | PROVEN LIVE (run `2026-07-18T03-44-26-136Z-77747146`): 400 {20007 expired} — proofs back via verified-UNPAID recovery, single intercept; final 18 (execute-swap fee), delta 0±1 |
| `fault.melt.wrong-shape-200`           | JF    | product-run                        | PROVEN LIVE (run `2026-07-18T03-50-48-989Z-041dc423`): 200 + garbage body — ambiguous like timeout, no blind restore, verified-UNPAID recovery; final 18 (execute-swap fee), delta 0±1 |
| `fault.ecash-send.swap-11002`          | JF    | product-run                        | PROVEN LIVE (run `2026-07-18T04-43-06-604Z-a6b3789b`): swap 400 {11002 proofs pending} — proof-selector sheet surface, single intercept, delta 0, balance 19 |
| `fault.ecash-send.swap-11003`          | JF    | product-run                        | PROVEN LIVE (run `2026-07-18T04-49-36-109Z-5606708d`): swap 400 {11003 outputs already signed} — fails cleanly to the proof-selector sheet, balance conserved at 19 |
| `fault.ecash-send.swap-malformed`      | JF    | product-run                        | PROVEN LIVE (run `2026-07-18T04-56-13-766Z-4fe3f69f`): swap 200 + truncated JSON — ambiguous, never definitive-restore; checkstate-verified reconciliation lands 19; proof-selector sheet surface |
| `fault.ecash-receive.swap-11001`       | JF    | product-run                        | PROVEN LIVE (run `2026-07-18T05-19-15-804Z-fe732637`): fresh-₿0 wallet, trust granted live, redeem 400 {11001} is TERMINAL — no pending row, no credit, balance stays 0 |
| `fault.receive.unknown-mint-info-offline` | JF | product-run                        | PROVEN LIVE (run `2026-07-18T05-37-14-089Z-936ea4c4`): unknown-mint trust review with the whole mint dead — getMintInfo failure is swallowed, the trust sheet opens degraded (no crash, no stuck spinner), nothing credited, no half-trusted mint, balance 0 |
| `fault.ws.dead-polling-fallback`       | JF    | product-run                        | PROVEN LIVE (run `2026-07-18T05-42-26-269Z-79819f19`): NUT-17 socket forced dead — HybridTransport fast-polling (5s) settles the real 100-sat Sovran receive (+100 exactly once); success under degradation; normal sweep |
| `receive.lightning.sat`                | JF    | product-run                                  | Real 100-sat quote paid by cocod; +100 delta; exact sweep/reconciliation; final zero            |
| `receive.cashu.paste`                  | JF    | product-run                                  | Controlled 50-sat token pasted/redeemed; finalized Paste tx; +50; sweep; final zero             |
| `send.cashu.sat`                       | JF    | product-run                                  | Fund 100; pending toast/tx; redeem/finalize 40; named token QA shot; sweep 60; exact accounting |
| `send.lightning.sat`                   | JF    | product-run                                  | Fund 100; preview/pay 40; PAID; exact -40/no fee; sweep 60; final zero/reconciliation           |
| `recovery.reinstall`                   | JS    | product-run                                  | Reinstall RestoreGate and drawer profile-name continuity                                        |
| `receive.lightning.change-mint.amount` | JF    | product-run                                  | PROVEN LIVE (run `2026-07-18T05-51-23-237Z-7be54b02`): 125-sat amount survives the mint-selector round trip, the fresh quote names Minibits, cocod pays it, +125 settles at Minibits; swept (the earlier Minibits TLS blocker was external and is gone) |
| `receive.lightning.pending-relaunch`   | JF    | product-run                                  | Interruption axis PROVEN LIVE first try (run `2026-07-18T10-22-20-242Z-7524d8f3`): 25-sat quote proven UNPAID, sheet dismissed, cold relaunch (`reset:none`), THEN cocod pays — the resumed pending-quote watcher (`watchExistingPendingOnStart`) settles the SAME pre-relaunch transaction: confirmed toast renders on the wallet home (root `E2EToastProbe` reachable there), View routes to the finalized original tx, +25 exactly once, swept/reconciled. New `check:interruption` facet documents the axis |
| `receive.cashu.dismiss`                | JF    | product-run                                  | Prepared 50-sat preview; Cancel; zero mutation; recover unspent token; final zero               |
| `receive.npc.sat`                      | JF    | superseded (fully)                           | Superseded by `receive.npc.default-mint` / `receive.npc.change-mint` product-runs (LNURL pay → confirmed toast → tx detail incl. npc mint truth) and, for its last unique claim (View-button routing on the npc toast), by the `receive.npc.toast-view` product-run |
| `receive.lightning.qr`                 | JF    | product-run                                  | QR-displayed invoice (no copy); processing/confirmed/View toast, finalized tx, +33, sweep (renamed from `toast.receive-lightning`; relaunch/no-replay coda removed) |
| `send.cashu.change-mint.amount`        | JF    | product-run                                  | PROVEN LIVE (run `2026-07-18T06-35-57-757Z-4280f8c6`): Create Ecash preselects preferred Sovran, typed 40 survives the amount-screen mint switch to Minibits (`amount-selected-mint` flips while `amount-state:40` stays visible), token minted from Minibits and redeemed by cocod (finalized), delta -40±3, dual sweep. P2PK-lock survival remains the separate `PAY-5.1-P2PK-MINT` backlog row |
| `send.cashu.pending-reclaim`           | JF    | product-run                                  | Pending 40; Check Status; cancel/reclaim; rolledBack; value conservation; exact sweep           |
| `send.cashu.pending-relaunch`          | JF    | product-run                                  | Funds-safety interruption axis on send PROVEN LIVE first try (run `2026-07-18T19-42-28-743Z-35fa1ef1`, funds reconciled): fund 100, create a 40-sat pending token, cold-relaunch (reset:none) as a force-quit — balance holds at 60 across the restart (pending token survives, no loss/double-debit), the persisted pending tx is still reclaimable (cancel → rolledBack → 100), swept. Symmetric to `receive.lightning.pending-relaunch` |
| `send.token.actions`                   | JF    | product-run                                  | Copy text/emoji, share, QR controls, status, cancel/reclaim, exact sweep                        |
| `send.lightning.change-mint.preview`   | JF    | product-run                                  | PROVEN LIVE (run `2026-07-18T06-11-18-806Z-af4cb5d0`): 40-sat Sovran preview switched to Minibits at confirmation — destination fingerprint and amount survive (`melt-destination-fingerprint` + `melt-selected-mint:` probes), paid once from Minibits, dual sweep |
| `send.lightning.dismiss`               | JF    | product-run                                  | Cancel unpaid synthetic preview; no Pay/settlement; delta 0; no melt row; exact sweep           |
| `payment.request.delivery-rollback`    | JF    | product-run                                  | 30-sat delivery failure; rolledBack; persisted detail; delta 0; exact sweep                     |
| `tx.source.cashu-paste`                | JF    | product-run                                  | Prepared source-less preview; distinct finalized Paste tx; persisted Clipboard detail; sweep    |
| `isolation.payment-roots`              | JS    | draft; deferred:redesign/root-gap            | Seed dirty context, invoke each actual root, and prove it clean immediately                     |
| `receive.lightning.change-mint.confirm` | JF   | product-run                                  | Quote-screen mint switch mints a fresh Minibits quote with the amount intact; +150; sweep       |
| `receive.cashu.unknown-mint`           | JF    | product-run                                  | Untrusted testnut token routes through mint-trust review; Add mint, redeem again; +10 gross (9 net of testnut fee); valueless principal auto-written-off, no sweep |
| `receive.cashu.unknown-mint.reject`    | JF    | product-run                                  | Same untrusted testnut entry, but the trust review is REJECTED (run `2026-07-17T07-42-43-268Z-5e9b15b4`): back to the untrusted token preview, cancel, mint selector proves testnut never added, delta 0; unredeemed valueless token auto-written-off, no sweep |
| `receive.lightning.back-reentry`       | JS    | product-run                                  | Dead-Next back-nav regression guard (395889e1), run `2026-07-17T08-09-25-510Z-7154dc0c`: 21-sat UNPAID Sovran quote, flow-header-back pops to the receive hub (hub restarts fresh — reset:true), re-enter Fixed Amount, 210, Next re-opens the variants menu a SECOND time and mints a fresh 210 UNPAID quote; both dismissed, no funds |
| `receive.payment-request.sat`          | JF    | product-run                                  | Standing NUT-18 creq from the QR-display Cashu tab paid over its real nostr transport (run `2026-07-17T08-31-11-330Z-887aede1`): harness `paymentRequest.pay` op gift-wraps a cocod-minted 25-sat Sovran payload (NIP-17) to the nprofile relays; app auto-redeems, confirmed toast, +25 exact, sweep/reconciled |
| `settings.keyring.generate`            | JS    | product-run                                  | P2PK Keys screen for a fresh user (run `2026-07-17T08-48-04-987Z-2a7580b9`): exact auto-provisioned baseline YOUR KEYS (2) (identity nsec auto-imported + active receive key), Generate header action mints a third (count 3); popup-input import variant deferred (FWO input + keyboard-occluded submit) |
| `transactions.filter.payment-type`     | JF    | product-run                                  | Type filter on a Lightning-in + ecash-out history (run `2026-07-17T08-55-01-489Z-1184fe20`): Lightning chip hides the ecash send row, Ecash shows it (row visibility is the evidence — Type chips expose no AX state); reuses the proven direction-filter funding legs; -40 delta, sweep/reconciled |
| `transactions.filter.status`           | JF    | product-run                                  | Status filter with a deliberately-pending 40-sat ecash send (run `2026-07-17T09-05-58-620Z-0e93a018`): Pending shows the send row, Confirmed hides it; the pending token is then reclaimed from its wallet-home row (Cancel → cancelled toast), delta 0, sweep/reconciled |
| `send.mock-fail.lightning`             | JF    | product-run                                  | Melt-failure rescue (run `2026-07-17T09-30-52-197Z-b7656cd1`): Mock Fail Melt (new `settings-mock-fail-melt-toggle`) makes executeMelt throw after prepare; the melt screen transitions to the SEND • CANCELLED timeline ("Funds returned to your balance" — no toast fires), delta 0, unpaid invoice, sweep/reconciled |
| `settings.design-system.showcase`      | JS    | product-run                                  | Dev-mode Design System hub (run `2026-07-17T10-26-59-037Z-68bd9c4b`): Timeline family mounts its interactive state player (Cashu/Lightning/Onchain/Request rails, Pause/Step/Reset — paused for a stable frame); Wallet-controls family renders amount/keypad states at the top and copyable value/selection squares/payment details/transfer feedback after scroll; no funds |
| `transactions.filter.mint`             | JF    | product-run                                  | Mint filter on a two-mint history (run `2026-07-17T10-45-15-259Z-2db76ad2`; 50 Sovran + 50 Minibits in-txs): Minibits selection leaves exactly one mint row (captured), Sovran selection hides it; mint chips carry new `filter-mint-<url>` testIDs with AX selection state; dual sweep, reconciled |
| `transactions.filter.source`           | JF    | product-run                                  | Source filter over annotations (run `2026-07-17T12-04-29-107Z-1ae13b41`): Minibits Lightning funding (no scan source) + Sovran 30-sat paste redeem; Paste shows exactly the paste receive row, QR hides it; dual sweep, reconciled. This scenario EXPOSED and now regression-guards the annotation-wipe defect: pre-hashing `raw:<full-token>` keys (>1kB) failed the persisted 256-char key cap and createMergeWithSchema's whole-blob discard silently erased EVERY transaction annotation on the next launch after any cashu paste (proven via `store.tx_annotation.merge_rejected invalid_key` at each relaunch). Fixed by hashing `rawAnnotationKey` (sha256, bounded) + entry-tolerant annotation persistence that salvages valid keys from poisoned blobs |
| `mint.add.url`                         | JS    | product-run                                  | Manual URL add of testnut from the wallet-header mint selector (typed via HID input); persists and is selectable on reopen |
| `mint.split.zero-balance`              | JS    | product-run                                     | Split action at ₿0 still opens the distribution screen with all default mints and an enabled Next (percentage-based, not balance-gated) |
| `send.zero-balance`                    | JS    | product-run                                     | Send chooser opens unguarded at ₿0; Create ecash stops at the insufficient-balance toast (machine NO_BALANCE), never reaching the amount screen |
| `send.nutdrop.scanning`                | JS    | product-run                                  | Subsystem-unavailable axis on Nut Drop PROVEN LIVE first try (run `2026-07-18T19-55-53-392Z-b1e57340`): from the Send hub, `send-method-nutDrop` opens the NearPay radar which degrades to the dedicated 'Scanning nearby' BLE empty state (no nearby peers in the ephemeral sim) — not a crash/blank/stuck spinner; back to wallet. First proof the BLE-dependent Nut Drop surface renders SAFELY in sim (NUTDROP-ENV was flagged unknown — now the empty/no-peers state is confirmed sim-safe; actual BLE transport remains JP) |
| `send.cashu.preselect-funded-mint`     | JF    | product-run                                     | With preferred Sovran empty and Minibits funded, Create ecash opens preselected to Minibits (`amount-selected-mint` probe); full-balance 50-sat token redeemed by cocod drains to 0; sweep |
| `send.lightning.preselect-funded-mint` | JF    | product-run                                     | Pasted 30-sat bolt11 opens the melt preview on the funded Minibits mint (`melt-selected-mint` probe) while preferred Sovran is empty; settled after PAID; sweep |
| `send.lnaddress.preselect-funded-mint` | JF    | product-run                                     | Pasted cocod npc address opens the amount screen preselected to funded Minibits and carries it to the melt preview; dismissed (no cocod npc claim surface yet); delta 0; sweep |
| `send.lnaddress.npc.sat`               | JF    | product-run                                     | Normal npc-address paste on a funded Sovran wallet: amount entry, Sovran melt preview, dismissed pending an observable `npc.settled` op; delta 0; sweep |
| `receive.npc.default-mint`             | JF    | product-run                                     | QR display Lightning address rail; cocod pays 50 over LNURL; confirmed toast; wallet-list tx opens detail proving the default Minibits npc mint; sweep |
| `receive.npc.change-mint`              | JF    | product-run                                     | npc receive mint switched to Sovran via the new `receive-npc-mint-row` (server-persisted); cocod pays 50; tx detail proves Sovran, not Minibits; sweep |
| `wallet.currency.display`              | JF    | product-run                                  | Fund 200; display currency switched USD→GBP→EUR→USD via the fiat pill's native menu; screenshot evidence per symbol (live price, no exact-conversion assert); delta 0; sweep |
| `wallet.unit.switch-usd`               | JS    | product-run                                  | Unit-switcher pill native menu → USD account; `wallet-selected-mint` probe proves the preferred mint re-snaps to cubabitcoin (only default usd mint); switching back keeps cubabitcoin (it also has sat keysets — selectUnit retains a compatible mint) while the sat page returns |
| `receive.lightning.usd-dismiss`        | JS    | product-run                                  | USD account $0.05 fixed-amount Lightning quote at cubabitcoin renders a sat-denominated bolt11 with an UNPAID usd tx probe; dismissed — deliberately no payment (cocod is sat-only) |
| `mint.info.reviews`                    | JS    | product-run                                  | Mint selector 3-dots (`mint-inspect:*`) opens the Cuba Bitcoin mint page (coordinate tap — the 3-dots is AX-collapsed inside the row); the cached KYM aggregate hydrates the reviews headerRight; Reviews screen opens |
| `mint.info.send-message`               | JS    | product-run                                  | Mint page Nostr contact row → profile → Send Message action menu → Nostr DM conversation (`dm-chat-probe`) |
| `receive.qr-display.tabs`              | JS    | product-run                                  | QR display rails tour: Unified default, Lightning Address/BOLT 12 pills, bolt12+onchain find-mints empty states round-trip to pre-filtered Add Mints, Cashu tab P2PK lock toggled on (state probe 0→1) |
| `mint.add.multiple`                    | JS    | product-run                                  | Three discovery-catalog mints checked and batch-added in one confirm; all three rows persist in the selector |
| `mint.add.mixed-discovery-manual`      | JS    | product-run                                  | One discovery mint + manually-typed testnut URL checked together; a single confirm adds both |
| `send.search.npub`                     | JF    | product-run                                  | Header search resolves a typed npub to a profile; Send Money falls back to `<npub>@npubx.cash` (no lud16) with a "to npub.cash" menu variant; 40-sat melt proven PAID app-side, ledgered via `npc.outflow` (trusted delivery); sweep |
| `wallet.offline.banner`                | JA    | product-run                                     | CONVERTED to the android driver's REAL airplane mode (`network` step, `device.network` capability — the settingsStore.mockOffline toggle dance is retired from e2e): radios genuinely drop, the YOU ARE OFFLINE shell banner appears after the OfflineProvider's ~5-6.5s hysteresis and persists across wallet/drawer/settings; back online it clears near-instantly. Defers (◌) on the iOS sim |
| `receive.offline.lightning-quote`      | JA    | product-run                                     | Real airplane mode (android): Receive → Fixed 100 → Next with the whole device offline fails into `e2e-toast-mint-unreachable`, no invoice rendered, ₿0 preserved after recovery. Documents the missing offline preflight gate (post-hoc classification only); legs copied from `fault.mint-quote.create-offline` |
| `mint.offline.add`                     | JA    | product-run                                     | Real airplane mode (android): add-mint by URL fails soft with nothing persisted — the device-offline twin of `fault.mint-add.unreachable` (whose per-mint legs stay on iOS); back online the selector still lists no testnut row |
| `wallet.offline.nostr`                 | JA    | product-run                                     | Real airplane mode (android): drawer social routes (Feed/Contacts/Notifications) still navigate under the offline banner; screenshots DOCUMENT the spinner/empty degradation — these screens have no offline empty-state yet (product gap) |
| `onboarding.offline`                   | JA    | product-run                                     | Real airplane mode (android) armed after Terms: the whole intro carousel → Get Started → wallet completes offline (identity keys are local), banner asserted mid-carousel and at the wallet; documents that default-mint bootstrap does not block reaching the wallet home |
| `receive.offline.ecash-pending`        | JA    | product-run                                     | Funded (valueless testnut) android: fund 20 online, then paste+redeem a 10-sat token under REAL airplane mode → holds PENDING while the mint is unreachable (not lost), holds PENDING (Tap Redeem to add to wallet) and is LOST on relaunch (No History) — the recovery is a manual re-redeem online, which credits +10 face (net +9 after the 1-sat testnut fee). First Android exercise of the e2e clipboard bridge feeding the app's real Paste + the funded runtime (seed export over adb reverse, cocod counterparty) |
| `profile.other.view`                   | JS    | product-run                                  | Complement to `profile.own.view` PROVEN LIVE first try (run `2026-07-18T20-15-51-922Z-4bdee78c`): searching a counterparty npub and opening the result routes to ANOTHER user's profile (`user-profile:other` probe, NOT `:own`) where the Follow affordance IS present; renders without crash; back to wallet |
| `profile.own.view`                     | JS    | product-run                                  | Fresh-user own social profile PROVEN LIVE first try (run `2026-07-18T20-08-17-746Z-99a20b4e`): drawer name → `/(user-flow)/profile` opens the viewer's OWN profile (new `user-profile:own`/`:other` root probe — the only stable AX root since stats collide with the drawer and header actions are AX-collapsed) where Follow and Send-Money are correctly ABSENT (other-profile-only); renders without crash; back to wallet |
| `dm.send.nostr`                        | JS    | product-run                                     | Header search resolves cocod's npub; profile Send Message → Nostr DM (NIP-17) opens the 1:1 thread; an `[E2E]`-prefixed message typed into the composer renders as an outgoing bubble |
| `settings.seed.reveal`                 | JS    | product-run                                     | Settings → Account profile: NIP06 recovery phrase starts obscured, Show flips the reveal probe 0→1 and the button to Hide, Hide re-obscures — the backup read path round-trips |
| `drawer.navigation`                    | JS    | product-run                                     | Drawer menu sweep: Feed (For You), Contacts, Notifications, AI (composer), Remote Login (signer hub) each land on their surface for a fresh user, then back to wallet |
| `feed.thread.backnav`                  | JS    | authored (repro loop)                           | Feed→Thread→back ×3 with reply scrolling between: after each return the For You list must still expose post content (`post-zap` AX id) — the red-capable repro for the blank/corrupted feed after thread back-navigation (frozen FlashList under the thread modal) |
| `theme.wallpaper.apply`                | JS    | product-run                                     | Theme → gallery → first album card (built-in Colors gradient album) → Apply: the slug flips off none AND the image-render probe reads wallet-wallpaper-image:none, confirming the color album resolves to a gradient (not a broken image) |
| `theme.wallpaper.apply-second`         | JS    | product-run                                     | Same flow as theme.wallpaper.apply but taps the SECOND album card (Artemis II Collection); asserts the exact slug AND that the wallpaper IMAGE actually decoded and painted (wallet-wallpaper-image:loaded), not just that a slug persisted |
| `send.mock-fail.ecash`                 | JF    | product-run                                     | Fund 50, enable Mock Fail Send (dev), attempt a 10-sat Create Ecash: executeSend fails pre-prepare and degrades to the offline proof-selector sheet (Choose amount round-up/down); dismissed, balance stays 50 (delta 0) — failed sends debit nothing; sweep |
| `transactions.filter.direction`        | JF    | product-run                                     | Fund 100 (in-tx) + redeemed 40-sat ecash send (confirmed out-tx): View all → Filters → In hides the outgoing row (transaction-send-<id>), Out shows it; chips assert their AX selection state; sweep |
| `send.offline.ecash`                   | JA    | product-run                                     | CONVERTED to the android driver's REAL airplane mode and valueless testnut so the offline-persistence check carries no real-sat custody risk: fund 50 testnut online (→49 after fee), then under the offline banner Send-all composes a token from local proofs with no mint round-trip (pending out-tx asserted); back online the send remains pending and reopens from the wallet list, documenting that it is not auto-reclaimed |
| `wallet.offline.fiat`                  | JA    | product-run                                     | Real airplane mode (android): the wallet-fiat-pill keeps rendering the last cached price offline (PricelistProvider reads the persisted store; the rate fetch dies with the radios); DOCUMENTS the product gap that there is NO staleness indicator (isStale computed but never consumed). Fund-free (balance 0, asserts the fiat display persists) |
| `send.offline.lightning`               | JA    | product-run                                     | Real airplane mode (android, valueless testnut): paste a cocod bolt11 offline → the melt quote can't reach the mint → 'Could not connect to mint' toast, balance intact; same post-hoc classification gap as the receive side (no upfront connectivity gate) |
| `settings.notification-policy`         | JS    | product-run                                     | Settings → Notifications: Relaxed default (AX state), pick Strict (states flip), back — the Settings row description reads Notifications, Strict, proving the persisted enum round-trips |
| `signer.hub.paste`                     | JS    | product-run                                     | Remote Login → Paste Connection Link: a synthetic nostrconnect URI typed into the FWO input (typeText) opens the signer-connect review sheet (probe value), dismissed without approving — paste → validate → review pipeline |
| `wallet.scan.camera-permission`        | JS    | product-run                                     | First permission-axis scenario PROVEN LIVE (run `2026-07-18T08-32-43-905Z-08ae1cf0`; original menu-era proof `…07-33-38…0b9247d1`): camera TCC revoked via the new `permission` step (simctl privacy + relaunch), wallet Scan QR stops at the camera-permission blocked popup (`e2e-toast-camera-permission` — param popups now stamp their probe key, and sheet-variant popups mount the same marker) with NO `/camera` navigation; grant + relaunch recovers to the real camera screen (Scanning state); TCC reset in finally. The denied/blocked path now uses the FWO-safe popup instead of `actionMenuPopup`, whose sheet presents invisibly BEHIND pushed native routes (Menu.Portal must disable FullWindowOverlay) — that cross-cutting occlusion still affects other actionMenuPopup call sites reached from pushed routes |
| `receive.paste.invalid-clipboard`      | JS    | product-run                                     | Negative-input axis PROVEN LIVE first try (run `2026-07-18T07-41-05-041Z-899e2202`): Receive hub Paste with an EMPTY clipboard fails soft via `e2e-toast-no-clipboard-address`, and with plain garbage text via `e2e-toast-unsupported-input` (new bounded `setLiteralClipboard` step — non-payload literals only; `E2EToastProbe` now mounted in-sheet on ReceiveHubScreen); no navigation, no redemption, ₿0 intact |
| `wallet.deeplink.nostrconnect`         | JS    | product-run                                     | OS-level deep-link axis PROVEN LIVE (run `2026-07-18T07-54-00-891Z-180957f6`): `simctl openurl` (new scheme-allowlisted `openUrl` step, no payment payloads authorable) fires a synthetic `nostrconnect://` pairing URI at the warm app; the `+native-intent` rewrite lands on `/(signer-flow)/connect` and opens the signer-connect review sheet (`E2EActionMenuProbe` now mounted on the connect backdrop, value-pinned); dismissed without approving, wallet untouched at ₿0 |
| `transactions.filter.empty-result`     | JF    | product-run                                     | PROVEN LIVE first try (run `2026-07-18T09-18-26-600Z-eed5406b`): with exactly one incoming tx (100-sat Sovran funding), Direction=Out yields zero rows and renders the "No transactions found" / "Try adjusting your filters" guidance card (not a spinner, not a blank list); Direction=In recovers the funding row and the card disappears; delta 0, swept/reconciled. New `check:empty-state` facet documents the axis |
| `send.cashu.back-reentry`              | JF    | product-run                                     | PROVEN LIVE (run `2026-07-18T09-37-57-291Z-642bf1dd`): typed 40 in Create Ecash, flow-header-back to the send hub (tapUntil — single back taps get swallowed), re-enter — keypad restarts fresh (`amount-state:0`, so the typed 40 does not replay; the draft only stashes via the mint pill) and Next still mints a real token that cocod redeems to finalized. Send-side sibling of the receive dead-Next guard (395889e1); -40 delta, swept/reconciled |
| `wallet.location.permission`           | JS    | product-run                                     | PROVEN LIVE (run `2026-07-18T09-57-08-005Z-fb3e3bf3`): location-permission axis on the wallet home — Bitcoin Near You requests location on mount and degrades SILENTLY on denial (London default region), so the outcome now rides the card's aggregated AX element as `wallet-location:<status>` (iOS flattens every descendant into the card Pressable — an INNER probe view is AX-invisible there); granted baseline → revoke TCC + relaunch keeps the card rendering with the probe at denied (no crash/blank/prompt loop) → grant + relaunch recovers to granted; `permission` step enum extended with `location` (install-time auto-grant only runs on install, so revoke survives `launch reset:none`); no funds |
| `feed.following.empty`                 | JS    | product-run                                  | Fresh-user Following feed empty state PROVEN LIVE first try (run `2026-07-18T17-26-23-695Z-a917d7e0`): the Following tab's heroui Popular/Recent menu (now mirrored by the feed's `e2e-heroui-menu-open` probe, newly mounted + pinned) → blind Popular-row tap (y≈0.85) → the 'You're not following anyone yet' EmptyFeed no-follows card (label-matched); drawer.navigation only proved the For You tab |
| `contacts.groups.location-denied`      | JS    | product-run                                  | Permission × Groups combo PROVEN LIVE (run `2026-07-18T17-12-21-363Z-4871a67c`), asserting CURRENT behavior (fault-tbd): with location TCC revoked, the Groups tab shows 'Getting your location...' forever and no tier rows — `useLocationTiers` sets a permission error that ContactsScreen DROPS (only `tiers` destructured), so denied users get an eternal waiting message instead of permission guidance; a product fix should flip these asserts deliberately. GOTCHA: a `permission reset` in finally relaunches into a native camera prompt that blocks the AX tree — omit it (ephemeral sim is destroyed anyway) |
| `contacts.groups.mesh-chat`            | JS    | product-run                                  | Mesh tier tap → geohash channel PROVEN LIVE first try (run `2026-07-18T16-57-14-212Z-73fcc779`): the new `geohash-chat:<geohash>` probe (pinned) pins that the MESH channel specifically opened (bitchat public-broadcast surface); navigation/render assert only — deliberately no send (mesh messages are public); location fix seeded and cleared |
| `contacts.groups.tiers`                | JS    | product-run                                  | The Contacts Groups tab PROVEN LIVE (run `2026-07-18T16-47-27-948Z-cce77f60`; via the NEW `location` harness step — `simctl location set`, cleared in finally): with a seeded London fix the tab settles into the Bluetooth mesh tier (`contact-row:geohash:mesh`, deterministic) plus the geohash tiers (`contact-row:geohash:gcpvj0d` — precision-7 hash of the seeded coords; ContactRow derives these ids natively, no product change needed). PRODUCT FINDING kept open: without any fix the tab hangs on 'Getting your location...' indefinitely — `useLocationTiers` has no timeout/fallback and even the fix-independent mesh tier waits behind `getCurrentPositionAsync` (run `16-32-05` evidence) |
| `wallet.search.no-results`             | JS    | product-run                                  | The header search's no-match branch PROVEN LIVE first try (run `2026-07-18T16-22-40-057Z-8ec0537c`): a nonsense query settles past the loading skeletons into the dedicated 'No Results Found' guidance card (SearchResultRows → NoResultsFound, gated on settled non-trivial queries; static title is label-matchable) — dm.send.nostr / send.search.npub only proved successful npub resolution |
| `transactions.pending.batch-cancel`    | JF    | product-run                                  | Second reclaim surface PROVEN LIVE (run `2026-07-18T16-08-14-075Z-6c825948`): the Pending tab's 'Cancel 1 pending (20 SAT)' sweep footer (exact deterministic label) reclaims the outstanding ecash send in place — row gone into the empty-state card, full balance restored (₿30→₿50, delta 0), no double-reclaim, swept/reconciled. PRODUCT FINDING (asserted as current behavior): the batch path REMOVES the transaction record entirely — `assert tx` finds nothing afterward — while the single detail-menu cancel persists a `rolledBack` 'Cancelled / Funds returned' audit entry (`send.cashu.pending-reclaim`); the two reclaim paths should be reconciled deliberately. Also: never `stable`-screenshot the pending token screen (pulse) |
| `social.empty-states`                  | JS    | product-run                                  | Fresh-user social-surface truths PROVEN LIVE (run `2026-07-18T15-43-18-031Z-3b154783`; 4th attempt): Contacts is deliberately NOT empty — the default mints' Nostr contacts (Sovran/Minibits/Cuba) seed every filter (All/Recent/Mints all list them; the scouted 'Requests' filter does not exist on this build, so no reachable Contacts empty case) — while Notifications settles from its spinner into 'No notifications' via the new `notifications-empty:<variant>` id (viewer-ready branch, would fail loud on 'No profile'). New `contacts-empty:<filter>` id stays in product for future filter states. drawer.navigation asserted only nav titles |
| `ai.model.picker`                      | JS    | product-run                                  | AI model-selection affordance PROVEN LIVE (run `2026-07-18T19-20-57-866Z-eb37648c`) after a store-sidecar diagnosis: the model chip opens the model-picker FWO snapPoints sheet, now observable via `e2e-action-menu-open` (value model-picker). TWO-PART FIX: (1) the render marker's static key captured a stale openSeq (snapPoints sheets mount mid-settle) → keyed on live `popupOpenSeq` so it re-fires; (2) `E2EActionMenuProbe` was never mounted on the AI screen → added it. Also added `model-picker` to the probe sheetId set + PopupHost present-gate, and exported the render store into the e2e state-mirror (reusable FWO-sheet debug). Dismissed without changing model; ₿0 |
| `ai.history.empty`                     | JS    | product-run                                  | AI conversation-history affordance PROVEN LIVE (run `2026-07-18T18-24-34-308Z-2e4a0be6`; 1st sim attempt was an infra 'simulator unavailable' flake, clean on re-run): the header-right clock (now AX-reachable via the new `ai-history-button` + generic `headerRight{AccessibilityLabel,TestID}` on `buildExpoRouterHeaderOptions`) opens the 'Conversations' heroui menu (mirrored by the AI screen's new `e2e-heroui-menu-open` probe) holding only the disabled 'No conversations yet' row for a fresh user; dismissed, ₿0. Confirms the heroui-menu probe works for actionMenuPopup on any screen it's mounted — the earlier model-picker failure is specific to that sheet's presentation |
| `ai.send.no-credits`                   | JS    | product-run                                  | First AI-surface behavior coverage PROVEN LIVE (run `2026-07-18T15-21-14-817Z-670c53c2`): a fresh user's composer send short-circuits at the deterministic pre-network `no-api-key` gate (`useAiSend`) with the probe-backed `e2e-toast-no-api-key`; screen stays alive, ₿0 untouched. AX LESSON: the AI screen's SwiftUI composer is visible in the CAPTURE ax tree but INVISIBLE to the driver's live SSE stream (label 'Message composer' never matches) — SwiftUI-hosted fields need the `typeText` coordinate-focus pattern + a probe-guarded blind send tap (safe here: each tap only re-fires the pre-network popup). The 402 reserve gate remains e2e-unobservable (actionMenuPopup without e2eProbeKey) — backlog if pursued funded |
| `mint.add.cancel`                      | JS    | product-run                                  | Cancel/dismiss axis on mint-add PROVEN LIVE first try (run `2026-07-18T17-50-46-210Z-5ae4618b`): search testnut in Add Mints, then Cancel WITHOUT confirming — pops back to the selector with testnut absent, and a fresh selector reopen confirms it was never persisted; distinct from `mint.add.url`/`mint.add.relaunch` which confirm |
| `mint.add.relaunch`                    | JS    | product-run                                  | Persistence axis on mint membership PROVEN LIVE first try (run `2026-07-18T14-58-11-492Z-930018d9`): testnut added by typed URL survives a cold `reset:none` relaunch — the reopened selector still lists it (mint membership spans the coco DB + mint metadata store; a mint dropped at restart would orphan later balances there; `mint.add.url` only proves same-session reopen) |
| `wallet.unit.relaunch`                 | JS    | product-run                                  | Persistence axis on the active unit PROVEN LIVE (run `2026-07-18T14-50-48-454Z-51cd4f35`): the USD account picked via the unit switcher survives a cold `reset:none` relaunch — the wallet boots back onto the USD page (cubabitcoin `wallet-selected-mint` probe, no sat-page fiat pill) instead of resetting to Bitcoin (mintStore `activeUnit`, tolerant-enum persisted). LESSON: native-menu row taps race the menu animation — tap the row via `tapUntil` guarded on the destination signal (a plain tap left the accounts menu open on the first run) |
| `wallet.currency.relaunch`             | JS    | product-run                                  | Persistence axis on the display currency PROVEN LIVE first try (run `2026-07-18T14-37-11-117Z-e297da96`): GBP picked via the fiat pill's native menu at ₿0 survives a cold `reset:none` relaunch, proven by the NEW `wallet-fiat-currency:<code>` sibling probe (the native pill exposes no machine-readable code — `wallet.currency.display` is screenshot-only with bare delays; the probe also gives future edits a real wait signal), then restored to USD; no funds |
| `settings.routing.navigate`            | JS    | product-run                                  | Previously-untested settings route PROVEN LIVE (run `2026-07-18T18-38-34-026Z-ca700986`): the Swap routing row opens the routing-preferences screen; its rows render with defaults for a fresh user (Min transfer amount, Max intermediaries), no crash; back to wallet. GOTCHA: `Section` titles are CSS-uppercased ('MIDDLEMAN ROUTING') but source is title-case — the `label:` selector is exact-match, so anchor on mixed-case content rows, not section headers |
| `settings.notification-policy.relaunch` | JS   | product-run                                  | Persistence axis on the persisted-enum settings pattern PROVEN LIVE first try (run `2026-07-18T14-23-20-569Z-8ff264dc`): Strict selected, cold `reset:none` relaunch, the Settings row still reads 'Notifications, Strict' and the policy screen rehydrates Strict=1/Relaxed=0 — settingsStore (the store whose wipe once cost terms/onboarding) survives restart |
| `theme.wallpaper.cancel`               | JS    | product-run                                  | Cancel/dismiss axis on the theme flow PROVEN LIVE (run `2026-07-18T18-06-21-061Z-e10af35b`; first attempt hit an unrelated flow.onboard 'No History' flake, clean on re-run): pick the Artemis II album (stages a draft), then `theme-cancel` instead of Apply — the draft is discarded and the wallet returns with `wallet-wallpaper:none` still set, no wallpaper committed; distinct from the apply/relaunch variants |
| `theme.wallpaper.relaunch`             | JS    | product-run                                  | Persistence axis PROVEN LIVE first try (run `2026-07-18T14-15-19-957Z-041a65e3`; new `check:persistence` facet): the applied Artemis II wallpaper survives a cold `reset:none` relaunch — exact album slug AND decoded/painted image probes re-assert post-boot, never the `none` fallback (whole-blob-discard regression guard on the theme store) |
| `receive.npc.toast-view`               | JF    | product-run                                  | The retired `receive.npc.sat` plan's last unproved claim PROVEN LIVE (run `2026-07-18T14-04-22-803Z-b13a2e11`): after cocod's 50-sat LNURL pay to the npub.cash address, the confirmed toast's View action routes directly to the settled incoming npc transaction detail (probe-asserted, mint.minibits.cash, +50, swept/reconciled). Required an `E2EToastProbe` fix: the `payment-status-view` proxy was a 1×1 first-child that lost WDA taps to overlapping later siblings (the address row's Copy swallowed every attempt) — now 24×24 with sibling-winning zIndex. Tap the toast IMMEDIATELY after the confirmed wait (an interposed screenshot burns the visible window; the retained probe keeps `assert visible` true after dismissal) |
| `receive.qr-display.offline`           | JS    | product-run                                  | io:display × offline combo PROVEN LIVE first try (run `2026-07-18T13-17-51-260Z-69f9e5a5`): with Mock Offline committed (YOU ARE OFFLINE banner), the standing QR display's Unified tab still renders its copyable payload and the Cashu tab renders the locally-encoded creq (P2PK probe present) — no crash/spinner/dead-end; toggled back online, wallet restored |
| `send.cashu.dm-contact`                | JF    | authored; deferred:dm-thread-broken          | Ecash-over-DM: destination-first Send resolves cocod's npub via the smart input (the new driver focus-probe made HID typing here safe — an unfocused npub's 'r' hit expo-dev-menu's reload), the no-lud16 contact's "Select option" menu ("as Ecash" y≈0.845; "as Lightning" disabled) mints a 20-sat bearer token, `giftWrapDM.published` (recipient + self wraps), app auto-lands in the thread. FOUR live runs (last `2026-07-18T12-22-30-537Z-2559ca1d`) then fail on `cashu-bubble-own`: the nagg dm app-view fetch fails repeatedly (`payment.dm.conversation.failed`), the sender's bubble never renders, and the thread decays to a fully blank screen — a real user-facing dead-end after sending money. Product fixes landed but unverified: `dmEchoStore` optimistic-echo seam (Colada → thread) + `cashu-bubble-own/incoming` testIDs. Also unasserted gap: own-bubble "Cancel" navigates to /receiveToken instead of rollback. Funds reconciled every run |
| `profile.switch.create`                | JS    | product-run                                  | Multi-identity axis PROVEN LIVE (run `2026-07-18T10-43-29-956Z-59aa80c8`): drawer profile switcher → Generate new account restart-switches into a fresh derived profile (own empty wallet, different word-pair identity, profile 1's captured name absent from the new drawer), then the inactive-profile avatar (`drawer-profile-switch-0`) switches back and profile 1's name returns exactly. New `drawer-profile-switcher-open` / `drawer-profile-switch-<index>` testIDs. AX finding: the heroui switcher sheet's rows (`profile-create`, `profile-row-*`, `profile-tab-*`) ARE AX-addressable when presented over the drawer/base route — no coordinate tap needed; the `e2e-heroui-menu-open` probe on WalletScreen is NOT reachable there (the drawer occludes WalletScreen's whole AX subtree, and re-taps dismiss-oscillate the sheet) |
| `wallet.deeplink.camera-permission`    | JS    | product-run                                     | Deep-link × permission composition PROVEN LIVE (run `2026-07-18T08-37-17-565Z-95d672a8`): with camera TCC revoked, `sovran://camera` lands on the camera route's own 'Camera permission required' empty state (never a broken viewfinder); Grant access surfaces the camera-permission blocked popup with Open settings, and the route stays alive — no crash, no dead-end. Authoring this scenario EXPOSED + FIXED two shipped defects: (1) the Grant access label rendered white-on-white (Text primitive's inline base color beats className — fixed via the `color` prop), and (2) the old action-menu feedback presented invisibly behind the pushed camera route (silent dead-end — fixed by switching the hook to the FWO-safe `camera-permission` popup with an iOS-Settings deep-link button) |

## `STATE_MACHINE.md` section 5 — shipped flow matrix

### 5.1 Send ecash

| Contract                                                                | WV evidence                                                                                                                                         | AJ evidence                                                                                                       | JSON/native decision                                                                                                                                                      |
| ----------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Start, preferred/no-preferred mint, amount, exact/non-exact online send | `wallet/__tests__/flows/ecash-send.test.ts` (`ECASH_SEND_HAPPY`, no-preferred, non-exact cases); `wallet/__tests__/unit/default-operations.test.ts` | Amount/transaction presentation remains in existing app tests                                                     | `send.cashu.sat` passed its funded preferred-mint 40-of-100 product path; other matrix cases remain WV-owned                                                              |
| Change mint during amount entry                                         | `wallet/__tests__/flows/ecash-send.test.ts` (`ECASH_SEND_CHANGE_MINT`)                                                                              | `app/__tests__/amountDraftStore.test.ts`; `app/__tests__/mintSelectorStickyItems.test.ts`                         | `send.cashu.change-mint.amount` product-run proves amount survival through the live mint change                                                                                                                              |
| Preserve P2PK lock through mint change                                  | `wallet/__tests__/flows/ecash-send-p2pk.test.ts` (`keeps the lock through a mid-flow mint change`)                                                  | `app/__tests__/creqMintSelection.test.ts`                                                                         | Same deferred scenario; device must assert public lock annotation, never private material                                                                                 |
| Offline exact/round fallback and proof composition                      | `wallet/__tests__/property/offline-composition.test.ts`; `wallet/__tests__/unit/offline.test.ts`; ecash-send fallback cases                         | `app/__tests__/offlineReachability.test.ts`                                                                       | Representative offline UI scenario planned; no duplicate device matrix                                                                                                    |
| Pending, check status, cancel/rollback/reclaim                          | `wallet/__tests__/screen-actions/defaultHandlers.test.ts` (`cancel` success/offline/failure); `wallet/__tests__/unit/machine-effects.test.ts`       | `app/__tests__/menuActionFailures.test.tsx`; timeline snapshots in `app/__tests__/timelineScenarioProps.test.tsx` | `send.cashu.sat` proves automatic finalization after redemption; `send.cashu.pending-reclaim` proves Check Status plus cancel/rollback/reclaim and value conservation     |
| Token copy/share/emoji/QR controls                                      | Screen-action availability/handlers under `wallet/__tests__/screen-actions/`                                                                        | Component/action behavior tests where present                                                                     | `send.token.actions` product-run proves the named action matrix, pending status, cancel/reclaim persistence, final wallet, and exact sweep; blanket parity is not implied |

### 5.2 Lightning melt / pay

| Contract                                                         | WV evidence                                                                                                                           | AJ evidence                                                                        | JSON/native decision                                                                                                                                  |
| ---------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------- |
| Fixed BOLT11, amountless target, Lightning address/LNURL routing | `wallet/__tests__/flows/lightning-melt.test.ts`; `wallet/__tests__/flows/manual-entry.test.ts`; `wallet/__tests__/unit/lnurl.test.ts` | Send destination/scan tests under `app/features/send/__tests__/`                   | `send.lightning.sat` product-run proves the fixed 40-sat invoice path; amountless/address routing remains pure-layer owned                            |
| Preferred-mint selection and change mint mid-flow                | `wallet/__tests__/flows/lightning-melt.test.ts` (`uses preferred mint`, `allows mint change during flow`)                             | Mint selector/amount draft app tests                                               | `send.lightning.change-mint.preview` (`deferred`)                                                                                                     |
| Fee quote, execute failure reservation rescue                    | `wallet/__tests__/unit/default-operations.test.ts` (`cancels the operation when execute throws`)                                      | `app/__tests__/onchainMeltTimelineState.test.ts` for presentation precedence       | Pure coverage owns the failure matrix; one rendered rollback case belongs in the deferred scenario                                                    |
| Cancel/dismiss unpaid preview                                    | `wallet/__tests__/screen-actions/defaultHandlers.test.ts` melt cancel cases                                                           | `app/__tests__/lightningSendDismiss.test.tsx`; timeline/history presentation tests | `send.lightning.dismiss` product-run proves synthetic-preview Cancel, no payment/settlement, zero wallet delta, no melt row, exact sweep, and cleanup |

### 5.3 Mint / top-up (Lightning receive)

| Contract                                                             | WV evidence                                                                                       | AJ evidence                                                                 | JSON/native decision                                                               |
| -------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------- | ---------------------------------------------------------------------------------- |
| Preferred/no-preferred mint, quote creation, paid finalization       | `wallet/__tests__/flows/mint-quote.test.ts`; `wallet/__tests__/unit/receive-flow.test.ts`         | Receive rail mapping in `app/features/receive/lib/receiveRailItems.test.ts` | `receive.lightning.sat` has passing funded product evidence                        |
| Change mint and preserve amount                                      | `wallet/__tests__/flows/mint-quote.test.ts` (`MINT_QUOTE_CHANGE_MINT`)                            | Amount draft and selector tests                                             | `receive.lightning.change-mint.amount` product-run proves amount survival and the fresh Minibits quote live  |
| Back/re-entry, stale quote result, concurrent new choice             | `wallet/__tests__/flows/back-nav-reentry.test.ts`; `wallet/__tests__/flows/interruptions.test.ts` | App route behavior where present                                            | Keep exhaustive at WV; `receive.lightning.back-reentry` product-proves the simulator back/re-entry path (quote → back → hub restart → second variants-menu open → fresh quote); `receive.lightning.pending-relaunch` product-proves the relaunch-resume path (UNPAID quote survives a cold relaunch and settles via the resumed watcher when paid after boot) |
| Rendered processing/confirmed/no-replay toast and transaction detail | Machine notification order in mint/payment flow tests                                             | `app/__tests__/paymentToastGating.test.ts`; transaction/timeline tests      | `toast.receive-lightning` product-run proves the complete rendered lifecycle       |

### 5.4 Receive / redeem token

| Contract                                                        | WV evidence                                                                                        | AJ evidence                                                                                                                                | JSON/native decision                                                                                                                                                                                                                   |
| --------------------------------------------------------------- | -------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Decode, trust review, redeem, invalid/spent failure             | `wallet/__tests__/flows/receive-token.test.ts`; `wallet/__tests__/unit/default-operations.test.ts` | Receive screen/component tests                                                                                                             | `receive.cashu.paste` product-run proves the controlled trusted-mint Paste/redeem happy path, finalized transaction truth, balance conservation, and cleanup; trust-review plus invalid/spent rendered cases remain unproved on device |
| Offline DLEQ all-proof verification                             | Wallet offline/token unit and integration tests                                                    | `app/__tests__/offlineReceiveDleq.test.ts`; `app/__tests__/sovranReceiveDleqBoundary.test.ts`; `app/__tests__/nutDropDleqBoundary.test.ts` | WV/AJ own the proof matrix; device owns one valid and one rejected rendered case later                                                                                                                                                 |
| Preview dismissal without redeeming                             | Parser/flow state remains WV-owned                                                                 | UI dismissal/history behavior                                                                                                              | `receive.cashu.dismiss` product-run proves a prepared controlled preview, explicit Cancel, zero balance/history mutation, recoverable unspent custody, exact cleanup, and final wallet zero                                            |
| Source propagation for Paste/QR/gallery/deep-link/NFC/BLE/Nostr | `wallet/__tests__/unit/annotations.test.ts`; screen-action entry matching                          | App scan/deep-link/NFC adapters                                                                                                            | `tx.source.cashu-paste` product-run proves the first canonical source case from entry through immediate and reopened transaction detail; others remain explicit backlog rows                                                           |

### 5.5 Payment request (NUT-18)

| Contract                                                      | WV evidence                                                                                                             | AJ evidence                               | JSON/native decision                                                             |
| ------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------- | ----------------------------------------- | -------------------------------------------------------------------------------- |
| Parse fixed/open request, constrain mints, confirm and notify | `wallet/__tests__/flows/payment-request.test.ts`; `wallet/__tests__/unit/payment-request.test.ts`; codec contract tests | `app/__tests__/creqMintSelection.test.ts` | `receive.payment-request.sat` product-proves the receive-side transport success (standing creq → NIP-17 delivery → auto-redeem); send-side success still needs a controlled request fixture |
| Nostr/HTTP/in-band delivery                                   | `wallet/__tests__/unit/default-operations.test.ts` payment-request transport groups                                     | App Nostr transport remains app-owned     | `receive.payment-request.sat` exercises the real nostr transport end-to-end; the harness wrap is encrypted to the ephemeral test wallet's pubkey and `[E2E]`-memoed, so the public-relay write leaks nothing |
| Delivery failure cancel/reclaim and `rolledBack` routing      | `wallet/__tests__/unit/machine-effects.test.ts`; flow failure notification cases                                        | Timeline/history tests                    | `payment.request.delivery-rollback` product-run: rollback and persisted detail   |
| Multi-option BIP-321 fallback                                 | `wallet/__tests__/flows/bip321-multi-option.test.ts`; `wallet/__tests__/unit/bip321-unified.test.ts`                    | App chooser/source surface                | Keep exhaustive at WV; one native chooser/fallback smoke after driver support    |

### 5.6 NFC payment-request send

| Contract                                                | WV evidence                                                                                                                                                                                                                                                       | AJ evidence                                                                                                                                                                                                        | JSON/native decision                                                                                                                                         |
| ------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Auto-execute, writeback, rollback on non-stale failure  | `wallet/__tests__/unit/machine-effects.test.ts` tests `creates an NFC token, writes it back, and returns success intents`, `rolls back and releases the NFC session when write-back fails`, and `returns stale before writing when NFC send resolves after reset` | `app/__tests__/nfcNdefDecode.test.ts` owns NDEF decoding; `app/__tests__/nfcWriteOrdering.test.ts` test `writes zero NLEN, contiguous body chunks, and the real NLEN strictly last`                                | No NFC JSON scenario was authored in this slice. Pure adapter/effect coverage is not simulator, radio, or two-device evidence.                               |
| Bearer downgrade consent                                | No physical-transport claim follows from wallet effect tests                                                                                                                                                                                                      | `app/__tests__/confirmBearerDowngrade.test.ts` tests `resolves true only from the explicit Send unlocked action`, `resolves false from the explicit Cancel action`, and `fails closed when the sheet is dismissed` | AJ proves the consent callback contract only. No simulator or physical confirmation was run.                                                                 |
| Tag loss, timeout, session release, no double execution | The cited effect tests prove release on writeback failure and a stale-before-write guard; they do not prove physical tag loss or timeout                                                                                                                          | NDEF parsing and write ordering do not prove native session timing                                                                                                                                                 | Explicit JP gap until a separately approved physical harness exists; section 11.13 remains a product gap and no expected-success simulator case is authored. |

## `STATE_MACHINE.md` section 11 — cross-flow gaps and intended contracts

This table is deliberately complete. `Target` text in section 11 is not shipped
behavior and must never be represented as an expected-success device scenario.

| Section                                               | Current contract                 | Existing evidence                                                                                                                            | Coverage decision                                                                                                                                                                                      |
| ----------------------------------------------------- | -------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 11.1 Mint offline while device online                 | partial                          | `wallet/__tests__/flows/ecash-send.test.ts`; `wallet/__tests__/unit/machine-effects.test.ts`; `app/__tests__/offlineReachability.test.ts`    | WV covers present fallback. Add a rendered reason scenario only after the product carries a reason field.                                                                                              |
| 11.2 Any ecash send error falls back offline          | partial                          | ecash-send and machine-effects failure cases                                                                                                 | Keep red/contract cases at WV; no expected-success JSON for the exact-match non-mint error gap.                                                                                                        |
| 11.3 Payment request while mint offline               | gap                              | payment-request success/failure tests show current behavior                                                                                  | Product gap. No success scenario until offline-aware request execution ships.                                                                                                                          |
| 11.4 Receiver mints/request availability              | multi-option shipped, direct gap | `wallet/__tests__/flows/payment-request.test.ts`; `app/__tests__/creqMintSelection.test.ts`                                                  | WV/AJ own filtering; later chooser smoke verifies disabled reason.                                                                                                                                     |
| 11.5 Fiat fee-aware/offline/silent clamp              | partial                          | `wallet/__tests__/unit/mint-selection.test.ts`; `wallet/__tests__/property/offline-composition.test.ts`                                      | Unit/fee model stays WV. Live USD remains deferred because cocod is SAT-only.                                                                                                                          |
| 11.6 Round dialog destination scope                   | shipped                          | ecash-send/lightning-melt fallback cases                                                                                                     | WV exhaustive; representative device proof-selection smoke later.                                                                                                                                      |
| 11.7 BIP-321 fallback after any chosen-option failure | partial                          | `wallet/__tests__/flows/bip321-multi-option.test.ts`; machine-effects                                                                        | Keep failing ecash/NFC branches visible as product gaps; do not claim fallback JSON success.                                                                                                           |
| 11.8 Lightning address to Nostr profile               | partial                          | `wallet/__tests__/unit/nip05.test.ts`; `wallet/__tests__/unit/lnurl.test.ts`; manual-entry flow tests                                        | Controlled identity fixture later; live public profile data is not a baseline.                                                                                                                         |
| 11.9 Change mint anywhere/filter propagation          | partial                          | ecash-send, lightning-melt, mint-quote, P2PK, receive-method and NPC selector flow tests; mint-list enrichment                               | Receive mint-change now has current native seams but awaits a reachable Minibits product run; send mint-change still exposes native gaps. Under-filtered fallback rows remain a WV product regression. |
| 11.10 Option recommendations                          | shipped/emergent                 | `wallet/__tests__/unit/annotate.test.ts`; BIP-321 flow tests                                                                                 | WV owns recommendation matrix; one chooser rendering checkpoint later.                                                                                                                                 |
| 11.11 Auto-advance from insufficient selected mint    | partial                          | `wallet/__tests__/flows/ecash-send.test.ts`; `wallet/__tests__/flows/lightning-melt.test.ts`; `wallet/__tests__/unit/mint-selection.test.ts` | WV owns current redirect; future target gets a red test before code.                                                                                                                                   |
| 11.12 Lightning failure proposes another mint         | gap                              | current melt failure/cancel tests prove no alternate-mint routing                                                                            | Product gap. `send.lightning.change-mint.preview` does not claim this target.                                                                                                                          |
| 11.13 NFC Lightning fallback/offline range            | gap                              | current NFC effect + NDEF/write tests                                                                                                        | Product gap plus JP transport deferral; no expected-success simulator case.                                                                                                                            |
| 11.14 Onchain                                         | mostly gap                       | `wallet/__tests__/unit/default-operations.test.ts` onchain contract pieces; `app/__tests__/onchainMeltTimelineState.test.ts`                 | Keep helpers/timeline at WV/AJ. Live send/receive deferred until method implementation and controlled chain backend.                                                                                   |
| 11.15 Review mint “don't trust” auto-melt             | gap                              | `wallet/__tests__/flows/receive-token.test.ts` proves current trust-before-redeem flow                                                       | Auto-melt remains a product gap. `receive.cashu.unknown-mint.reject` product-proves the current reject/back path: reject returns to the untrusted preview, no mint added, no redemption.               |
| 11.16 Change mint at confirmation                     | partial                          | lightning-melt/payment-request/back-reentry tests                                                                                            | `send.lightning.change-mint.preview` exposes the app-global affordance and silent-confirm-after-dismiss gap without declaring it fixed.                                                                |

## Cross-cutting payment, lifecycle, and transaction coverage

| Contract                                                                                                      | Pure evidence                                                                                                                                                                                | JSON/native status                                                                                                                                                                                                |
| ------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Root-entry context clearing: Send, Receive, QR, NFC, mint selector, NutDrop, contact/chat, Routstr, deep link | `app/__tests__/clearPaymentContext.test.ts`; `app/__tests__/paymentRootContext.test.ts`; `wallet/__tests__/flows/interruptions.test.ts`                                                      | `isolation.payment-roots` needs redesign and per-root adapters/probes; Routstr top-up currently enters directly without clearing prior context or resetting Colada                                                |
| Reinstall seed-race classification                                                                            | `app/__tests__/appGateReinstallDetection.test.tsx`; secure-storage lifecycle tests                                                                                                           | `recovery.reinstall` product-run: same-simulator app reinstall, RestoreGate completion, exact drawer profile-name continuity, and final wallet state                                                              |
| Destructive funded recovery and proof restoration                                                             | wallet restore/counter tests + app secure-storage/key derivation tests                                                                                                                       | No JSON scenario; app-data erase versus keychain custody and local-history versus restored-proof truth remain explicit gaps                                                                                       |
| NPC mint selection and persistence                                                                            | `wallet/__tests__/flows/npc-mint-selector.test.ts`; screen-action handler tests                                                                                                              | `receive.npc.sat` deferred; processing/confirmed/View assertions required                                                                                                                                         |
| Toast gating/order/replay                                                                                     | wallet notification flow tests; `app/__tests__/paymentToastGating.test.ts`                                                                                                                   | `toast.receive-lightning` is a product-run; `receive.npc.sat` remains deferred                                                                                                                                    |
| Transaction state/timeline/rollback rendering                                                                 | `wallet/__tests__/unit/timeline.test.ts`; `wallet/__tests__/unit/timeline-engine.test.ts`; `app/__tests__/historyEntryTimelineBuild.test.ts`; `app/__tests__/timelineScenarioProps.test.tsx` | Every money JSON must eventually capture row → detail → source → wallet                                                                                                                                           |
| Transaction source annotations                                                                                | `wallet/__tests__/unit/annotations.test.ts` (incl. real coco-v2 id shapes + bounded raw-key hashing); `wallet/__tests__/screen-actions/entry-matching.test.ts`; `app/__tests__/transactionAnnotationStorePersistResilience.test.ts` (entry-tolerant persistence) | `tx.source.cashu-paste` + `transactions.filter.source` are product-runs; QR, gallery, deep link, NFC, BLE, Nostr, NPC, onchain, payment request/BIP-321, and split remain separately attributable cases. KNOWN GAP: a receive that goes pending first (offline/P2PK) finalizes via recovery without `onTransactionCreated`, so its scan annotation is never bridged — pending-path source annotation remains unproven |
| P2PK receive lock and key behavior                                                                            | `wallet/__tests__/unit/annotations.test.ts`; `app/__tests__/sovranPaymentConfig.profile.test.ts`; `app/__tests__/nutCreq.test.ts`                                                            | Controlled P2PK generation, mode-`0600` private custody, public lock indicator, propagation, and host sweep now exist; `P2PK-RECEIVE-ANNOTATION` remains a JF backlog until locked receive/rotation behavior runs |
| Onchain receive versus send                                                                                   | Receive: receive-method/default-handler/mint-quote tests. Send: default-operation/availability/melt-timeline tests. Both have explicit transaction-detail route tests                        | `ONCHAIN-RECEIVE` and `ONCHAIN-SEND` remain distinct live-backend/control gaps; neither direction stands in as evidence for the other                                                                             |

## Mint, multi-unit, split, offline, and proximity

| Surface                                            | Pure owner/evidence                                                                                              | Native/live decision                                                                                                                                                                                                                                                                                          |
| -------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `MINT-ADD-PERF` add/search/validation/info         | app mint hook/screen tests where present; mint capability tests in wallet                                        | Measure MintAddScreen p50/p95 and cover invalid, slow, duplicate, batch, partial, and retry outcomes after stable controls exist; optimize the implementation rather than masking latency with larger timeouts                                                                                                |
| `MINT-ADD-FIRST` deterministic first-result choice | Current network search order is not a fixture contract                                                           | Deferred until a controlled, sorted result fixture exists. “Pick the first mint” must mean the fixture's deterministic first row, never arbitrary live-network ordering                                                                                                                                       |
| `MINT-CAPABILITY-PAIRWISE` method/unit filtering   | `wallet/__tests__/unit/mint-capabilities.test.ts`; `wallet/__tests__/unit/mint-selection.test.ts`                | Capability-driven pairwise cases only; do not encode one mint's current advertised features as permanent product truth                                                                                                                                                                                        |
| `MINT-FEE-MATRIX` fee-bearing/fee-free flows       | wallet fee, quote, mint-selection, and conservation tests                                                        | Keep receive and send as an explicit pairwise WV/JF matrix: fee-free versus fee-bearing capability fixtures, displayed fee envelope, final conservation, and mint changes at every selector-bearing step. The live fixture remains deferred and must be capability-controlled, never a hard-coded public mint |
| `MULTI-UNIT-MATRIX` per-unit receive/send          | wallet unit/balance tests; app unit store tests                                                                  | Discover a multi-unit mint by capability at run time, then repeat receive/send and unit/transaction assertions for every supported unit. Cuba Bitcoin is a candidate fixture, not a hard dependency. Non-SAT live work needs a controlled counterparty                                                        |
| `PREFERRED-MINT-BRANCHES` funded/unfunded          | preferred-mint and mint-selection flow tests                                                                     | Retain four explicit branches: preferred mint set/unset × selected mint funded/unfunded, including whether selection, insufficient-funds routing, fees, and confirmation differ                                                                                                                               |
| `SPLIT-2-3-MINTS` conservation                     | wallet/history grouping and app distribution/orchestrator tests; `app/__tests__/mintDistributionSelectors.test.ts` pins the balance-split selectors | `mint.split.consolidate`; product-run `2026-07-18T05-58-26-552Z-a73befbe` proves the 50/50 two-mint funding, Minibits toggled off (Sovran 100%), the live rebalance under the declared `funds.transfers` fee budget, fee-tolerant final delta, and dual sweep. Three-mint cases, partial failure, retry, and stranded intermediary value remain deferred |
| `NPC-ADDRESS-AMOUNT` cocod candidate               | The pinned `0.0.16` preflight detects `cocod npc address`; this is capability evidence, not a successful payment | The approved current-wallet boundary may exercise the SAT amount selector only after the deferred scenario is promoted. USD remains deferred unless both mint and counterparty capability are proven; never infer it from SAT support                                                                         |
| `OFFLINE-LANES` send/receive                       | wallet offline property/unit tests; app DLEQ/reachability tests                                                  | Injected/mock offline is deterministic UI/effect evidence; actual host/device network loss is a separate lane (the android `device.network` airplane-mode scenarios). Never report one as proof of the other                                                                                                  |
| `OFFLINE-RECOVERY` android                         | none — no seed-entry UI exists                                                                                    | NOT AUTHORABLE. The Android `recovery.offline` equivalent cannot be written: uninstall wipes the Keystore/secure-store, and the app has NO manual seed-phrase entry screen and no onboarding "recover existing wallet" path (SettingsRecoveryScreen only re-restores ecash from mints using the seed already in secure store). Needs a seed-import UI before any recovery-from-seed test — offline or otherwise — exists on Android                                    |
| `OFFLINE-MAP` android                              | none — Google Maps key gated                                                                                     | DEFERRED. `wallet.offline.map` needs `EXPO_PUBLIC_GOOGLE_MAPS_API_KEY` at bundle time or MapScreen renders the "map unavailable on Android" state (mapUnavailableOnAndroid) instead of the map, so `map-locate` never appears. Author once the e2e android build is given a Maps key; the offline-tile behavior is untestable until then                                                                                                                              |
| `OFFLINE-DM` android                               | `app/__tests__` whitenoise/DM unit tests                                                                         | DEFERRED. `dm.offline.send` blocked by the same product breakage that deferred `send.cashu.dm-contact` on iOS (post-send DM thread renders blank; nagg dm app-view failing). Author once the DM thread surface is fixed                                                                                        |
| `NUTDROP-ENV` NutDrop                              | `app/__tests__/nutDropDleqBoundary.test.ts` and wallet transport/effect tests                                    | Environment support is unknown. No executable JSON claim until the simulator/physical transport boundary and safe fixture are proven                                                                                                                                                                          |
| `BLE-PHYSICAL` BLE                                 | BLE adapter and wallet transport/effect tests                                                                    | Injected state is not physical proof; real discovery/delivery remains JP                                                                                                                                                                                                                                      |
| `NFC-PHYSICAL` NFC                                 | Exact pure effect, NDEF, write-order, and consent tests are named in section 5.6                                 | No NFC JSON scenario exists in this slice. App-context isolation is not simulator or physical transport proof; JP remains deferred                                                                                                                                                                            |

## Nostr/social and accessibility

- Feed/thread/notification mapping, publishing/outbox, NIP-17/04 crypto,
  Blossom, media metadata, polls, search/quick-pay, and NIP-46 policy matrices
  remain owned by app Jest and controlled fixtures. Generated test seeds normally
  have no Nostr history, so include deterministic empty-state fixtures as a first-
  class baseline. Exact device baselines never depend on changing public feeds.
- Controlled-relay JSON work remains deferred until the relay fixture and
  side-effect cleanup are explicit: feed/search/follow/relaunch, DM unread and
  pagination, note lifecycle, media/Blossom failure, polls, and NIP-46 smoke.
- AX capture is evidence, not an assertion. Every named visual checkpoint must
  assert the expected semantic screen/control state. Expand deterministic Design
  System/accessibility coverage before multiplying device pixel scenarios.

## Legacy `.sov` parity and retirement truth

JSON is the sole new functional/device authoring format. The old `.sov` source
corpus is no longer an execution dependency. Its useful behaviors informed the
four original payment definitions and the complete deferred plans in the current
inventory, but none establishes full parity and full `.sov` parity is not an
acceptance criterion for this slice. The explicitly legacy-derived cases are:

- Cashu receive dismiss: `receive.cashu.dismiss`.
- Cashu pending/cancel/reclaim: `send.cashu.pending-reclaim`.
- Lightning preview dismiss: `send.lightning.dismiss`.
- Copy/share/emoji/status/QR/cancel action matrix: `send.token.actions`.

The runtime under `app/codereview/log-doctor/test-dsl/` remains only because
log-doctor still imports it. It is not a second approved authoring format. Remove
that dependency in a separate log-doctor refactor after its non-test consumers
are decoupled.

## Coverage gates

1. Every `e2e/scenarios/*.json` file is referenced exactly once by `full.json`.
2. Suite ref id, file, lane, requirements, and end state match scenario metadata.
   The validator compares each ref with the scenario's effective requirements,
   including nested fixtures; cleanup is authored in scenario `finally` steps.
3. `default.json` contains only safe, unfunded, non-live scenarios.
4. `deferredReason` short-circuits before setup and emits a visible deferred result.
5. Every section 5 and section 11 row above has an owning test layer or an explicit gap.
6. A JSON scenario is promoted from deferred only after its selectors, fixture,
   assertions, and final-state plan exist. Simulator execution additionally
   needs destructive-reset approval and fresh per-scenario ephemeral-device
   ownership that cannot erase or reuse an existing simulator. Funded promotion
   additionally needs fund-loss acceptance, declared exact assets, pinned/unlocked
   current cocod, typed operations, mode-`0600` custody, a clean global lock/audit,
   host NUT-13 sweep, and exact final accounting. Live/physical promotion remains
   outside this approved boundary.
7. After authoring or changing JSON, re-open the named source and cited pure tests,
   validate, and dry-run. `onboarding.fresh` has simulator product evidence;
   `send.cashu.sat`, `receive.lightning.sat`, `receive.cashu.paste`,
   `receive.cashu.dismiss`, `toast.receive-lightning`,
   `send.cashu.pending-reclaim`, `send.token.actions`, `send.lightning.sat`,
   `send.lightning.dismiss`, `payment.request.delivery-rollback`, and
   `tx.source.cashu-paste` have funded simulator product evidence. No
   generic-live or physical scenario has passed.
8. The aggregate verification report runs wallet Vitest, app Jest, JSON validation,
   and offline harness checks separately; it must not imply that one runner
   covered another layer or that fake execution proves product behavior.
