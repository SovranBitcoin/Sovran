# Learn more https://docs.github.com/en/get-started/getting-started-with-git/ignoring-files

# log-doctor working file
log.txt

# WebDriverAgent bring-up log (start-wda.sh)
wda.log

# Phone-test runner, artefacts, DSL, and related scripts — all local-only
.screenshots/
tests/
.test-drafts/
# Ephemeral simulator inputs and captures. These can contain live payment data.
.device-artifacts/
scripts/start-wda.sh
scripts/dev.sh
docs/device-automation.md

# JSON-native e2e evidence: screenshots, ax snapshots, metro.log, and custody
# material (legacy-SEEDS.json + custody/ hold LIVE test mnemonics) — never commit
e2e/artifacts/

# BitChat / BLE local identity (contains a private key — never commit)
selfIdentity.plist

# Local scratch working dirs
__audits__/
__research__/

# dependencies
node_modules/

# build
/android/

# Expo
.expo/
dist/
web-build/

# Native
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# Metro
.metro-health-check*

# debug
npm-debug.*
yarn-debug.*
yarn-error.*

# macOS
.DS_Store
*.pem

# local env files
.env*.local

# typescript
*.tsbuildinfo

# @generated expo-cli sync-2b81b286409207a5da26e14c78851eb30d8ccbdb
# The following patterns were generated by expo-cli

expo-env.d.ts
# @end expo-cli
.env.local

/android/

.vscode
.env

# ios
# ios/*
# !ios/Sovran/Info.plist
# !ios/Sovran/Sovran.entitlements

# *.png
# *.mp4

/ios
/android
# Expo
.expo
dist/
web-build/

# Vendored upstream packages — checked-in build artifacts so EAS can install
# from a sibling-less tree. Sync via `bun run vendor:marmot-ts`.
!vendor/**/dist/
!vendor/**/dist/**

sovran.money/

# Cursor skills (avoid committing vendored git repos inside skills)
.cursor/skills/**
.cursor/skills
.cursor/skills/**/.git/
.cursor/skills/**/.git/**

# Apple widget target generated files (expo prebuild / apple-targets)
targets/widget/generated.entitlements
targets/widget/Assets.xcassets/AppIcon.appiconset/
targets/widget/Assets.xcassets/$*.colorset/

credentials.json
credentials/
