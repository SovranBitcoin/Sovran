/**
 * "View all" list for a receive rail (payment requests / onchain / bolt12).
 * Pushed from the receive rails' "View all" segment; tapping a paid row pushes
 * its transaction detail into this same stack (slides in from the side).
 */

import { ReceiveRailListScreen } from '@/features/receive';

export default ReceiveRailListScreen;
