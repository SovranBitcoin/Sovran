/**
 * @fileoverview BitChat Network (peer list) route
 *
 * Pushed from the Bluetooth-mesh chat screen's headerRight. Shows all
 * BLE peers currently known to the mesh, matching upstream bitchat's
 * `MeshPeerList` sheet.
 */

import NetworkSheet from '@/features/bitchat/screens/NetworkSheet';

export default NetworkSheet;
