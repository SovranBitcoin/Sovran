import type { ViewStyle } from 'react-native';

export const THREAD_CONNECTOR_LINE_STYLE = Object.freeze({
  width: 0,
  borderLeftWidth: 2,
  borderStyle: 'dotted',
  borderRadius: 1,
} satisfies ViewStyle);
