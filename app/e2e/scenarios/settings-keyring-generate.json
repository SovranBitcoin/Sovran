{
  "version": 1,
  "id": "settings.keyring.generate",
  "name": "Security keys: generate a second receiving key",
  "description": "The fresh-wallet keyring shows one key; tapping Generate adds a second and the list updates to show two. No payment is performed.",
  "details": "Settings > P2PK Keys waits for YOUR KEYS (2) (a fresh wallet holds its P2PK key and the imported Nostr key), then Generate P2PK key waits for YOUR KEYS (3). The two keyring captures use canonical page settings-keyring; occurrence 2 is the post-generation state. These assertions check the displayed counts, not the origin of individual keys or the regeneration preference.",
  "lane": "simulator",
  "tags": ["flow:wallet","check:p2pk-keys"],
  "requires": ["fresh-install"],
  "setup": [
    {"use":"flow.onboard"}
  ],
  "steps": [
    {"action":"screenshot","name":"wallet"},
    {"action":"tapUntil","sequence":[{"tap":{"id":"header-profile-avatar"}}],"until":{"id":"drawer-menu-settings"},"attempts":4,"settleMs":6000},
    {"action":"tapUntil","sequence":[{"tap":{"id":"drawer-menu-settings"}}],"until":{"label":"Swap routing"},"attempts":4,"settleMs":6000},
    {"action":"waitFor","selector":{"label":"Swap routing"},"timeoutMs":30000},
    {"action":"swipe","dir":"up"},
    {"action":"waitFor","selector":{"label":"P2PK Keys"},"timeoutMs":30000},
    {"action":"screenshot","name":"settings"},
    {"action":"tap","selector":{"label":"P2PK Keys"}},
    {"action":"waitFor","selector":{"label":"YOUR KEYS (2)"},"timeoutMs":30000},
    {"action":"screenshot","name":"settings-keyring"},
    {"action":"tap","selector":{"label":"Generate P2PK key"}},
    {"action":"waitFor","selector":{"label":"YOUR KEYS (3)"},"timeoutMs":90000},
    {"action":"screenshot","name":"settings-keyring"},
    {"action":"tap","selector":{"label":"Go back"}},
    {"action":"waitFor","selector":{"label":"P2PK Keys"},"timeoutMs":30000},
    {"action":"goHome"},
    {"action":"waitFor","selector":{"label":"Split"},"timeoutMs":45000}
  ],
  "verify": [
    {"action":"assert","that":"visible","selector":{"id":"wallet-send"}},
    {"action":"screenshot","name":"wallet"}
  ],
  "finally": [
    {"use":"flow.return-to-wallet"}
  ],
  "endState": "wallet"
}
