{
  "version": 1,
  "id": "capture.storage",
  "name": "Storage inventory without exporting data",
  "description": "Read storage key and file names on a disposable new profile. Do not export, copy, clear or enable logging.",
  "details": "The real screen lists key names and filenames, never stored secret values. Waits for the inventory read to succeed and captures only the initial viewport. None of its dump/report/clipboard controls are pressed.",
  "lane": "simulator",
  "tags": ["flow:wallet","check:navigation"],
  "requires": ["fresh-install"],
  "setup": [
    {"use":"flow.onboard"},
    {"use":"capture.developer"}
  ],
  "steps": [
    {"action":"tapUntil","sequence":[{"tap":{"id":"settings-storage-row"}}],"until":{"id":"settings-storage-ready"},"attempts":4,"settleMs":6000},
    {"action":"waitFor","selector":{"label":"Storage Inventory"},"timeoutMs":30000},
    {"action":"screenshot","name":"settings-storage"},
    {"action":"goHome"}
  ],
  "verify": [
    {"action":"waitFor","selector":{"id":"wallet-send"},"timeoutMs":45000}
  ],
  "finally": [
    {"use":"flow.return-to-wallet"}
  ],
  "endState": "wallet"
}
