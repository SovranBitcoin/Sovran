# iOS Capture Refresh Attempt

Status: **focused native capture passed; unreviewed candidates imported; no
canonical pixels or pins replaced**. Build-bound native freshness and publication
approval remain unresolved. Dates in run IDs are UTC.

## Completed Focused Retry

The follow-up rechecked storage rather than carrying forward the earlier block:
checkout/temp/home had approximately **11 GiB** available. The standard wrapper
passed its 10 GiB reserve and native-artifact preflights. With a **20-minute tool
timeout**, this exact command ran only the requested scenario:

```sh
bun run press:capture ios --scenario marketing.screenshots
```

**Passed:** one scenario, zero failures/skips/deferrals, `funds: n/a`,
`proof: product-run`, **587,891 ms** (about 9 minutes 48 seconds). All 12 named
core-tour screenshots completed, followed by every verification step, Mock Mode
off assertions, scenario cleanup, and final `wallet` state. The wrapper exited
successfully and automatically imported the run through the existing intake.

Evidence, relative to `sovran-app/`:

- Run: `app/e2e/artifacts/run-2026-09-15T00-22-28-274Z-ae1c2dee/`.
- Candidate directory: `app/e2e/artifacts/run-2026-09-15T00-22-28-274Z-ae1c2dee/press/`.
- Candidate QR: `press/receive-qr.png` within that run.
- Original QR: `marketing.screenshots/named/receive-qr-004.png` within that run.
- `manifest.json`: exact `filters.scenario: marketing.screenshots`, dirty `main`
  at `fc597d4a23cf64be27e31e32a4480ba3d3c63c94`, source fingerprint
  `208864ce6dd27e55b4ea9cc3fa2a94db4556df26f595f7a2a230c52befe09f49`.
- `events.jsonl`: successful cleanup at line 297, final-state at 300,
  scenario end at 301, passing run end at 304.
- `session-1.json`: ephemeral iPhone 17 Pro, iOS 26.2, seed export disabled,
  no video, UDID `7C9612D9-B25F-4CC1-9CF0-975D959A5710`.

Intake validated all 12 named captures and retained **nine mapped original PNGs**:
mint-select, mint-info, receive-qr, send, dm-chat, feed, notifications,
ai-model-picker, and settings. Wallet, contacts and the first AI screenshot remain
in raw evidence; their press mappings belong to the store scenario, which was not
run. `press/manifest.json` remains `unreviewed-candidate` with native freshness
**unverified**, not promoted or certified by the diagnostic review below.

### Native Provenance Gap

The runner again installed the same existing bundle documented below into its
new disposable simulator. Its Info.plist version was rechecked as **0.1.1**;
the previously inspected build is **0.1.1 (1)**, Xcode 26.1.1 / 17B100,
iPhoneSimulator SDK 26.1 / 23B77. The current app package is **0.1.3**, served by
the run's owned Metro process. No native build was performed. A passing current
Metro journey does not bind the old native binary to current source, and the
dirty-tree fingerprint is not a clean release or immutable-source attestation.

### QR Diagnostic Review

Reviewed **only** the imported `press/receive-qr.png` (1206 x 2622), SHA-256
`b5dffbdfbea1e108c2228feb3be16d742ef7ec012810c6e1907a6e5a18ca29e5`.
The QR, Unified customization card, Advanced row and Copy footer are visible
without an error/permission overlay. The obsolete Onchain/BOLT12/Cashu pill row
is absent; the four current top-level tabs remain. No seed words, private key or
personal message is visible. The QR is still a disposable receive request, not
settlement, P2PK-lock, scanability or publication proof. No other new screenshot
received visual/privacy approval, and the candidate manifest was not relabelled.

### Cleanup And Scope

The runner reported deletion of its exact created UDID; a subsequent `simctl list`
query returned no matching device. Its recorded Metro PID 46550 and bridge PID
47689 were also absent in a subsequent process check. Post-run free space measured
**7.5 GiB**; it may recover as simulator resources are released. No further native
run or build was attempted, and nothing was deleted to free space.

The earlier incomplete-run/storage blocker no longer prevents having a completed
candidate set. Promotion still requires the existing build-provenance and review
gates. This follow-up changed only this report plus ignored runtime artifacts:
no registry, canonical screenshot, site, geometry, artwork, or store-pin edits;
no other scenario, funding, native build, external publication, or full E2E test
rerun. Verification was the real passing journey, automatic intake, manifest/event
inspection, QR diagnostic review, and owned-resource cleanup checks.

## Initial Attempt History

The remaining sections preserve the earlier interrupted attempt and its checks;
they do not describe the terminal status of the completed focused retry above.

## Source And Native Identity

- Historical receive source: `press/artwork/source/screenshots/ios/receive-qr.png`,
  registered to `run-2026-09-13T02-12-43-637Z-aca0af50`.
- Historical run source: dirty `9dffa086633993790a1ada8f3fe3e0a4259a661d`,
  fingerprint `a171a05dce5c71706a4f60989724263b40f6b7edf72a715eae5d55fea6e21fd0`.
- New run: `app/e2e/artifacts/run-2026-09-14T23-43-43-765Z-f96526dd/`.
- New run source: dirty `main`, `fc597d4a23cf64be27e31e32a4480ba3d3c63c94`,
  fingerprint `d2192dfcdf3918f66061fecae943f0a65ecad4e5fe4415a6cb3cffbaa014cc20`.
  The working tree includes concurrent app/copy/site changes. This is not a clean
  release revision or a claim that source remained immutable throughout capture.
- Current app package version: `0.1.3`. No `app/ios` project or Xcode DerivedData
  build product was present. No native build was performed.
- The framework selected an existing **bundle only**, not its user data, from
  `~/Library/Developer/CoreSimulator/Devices/2AB32C95-DD9E-42F5-8DD2-007935BDC43E/data/Containers/Bundle/Application/0C5A01E6-474F-4822-A10D-821C194E9E51/Sovran.app`.
- That bundle's Info.plist reports `com.sovranbitcoin.dev`, version **0.1.1 (1)**,
  iPhoneSimulator, SDK `iphonesimulator26.1` / `23B77`, Xcode `2611` / `17B100`,
  minimum iOS `16.4`. It passes the framework's baked-entitlements check.
- Host Xcode: **26.1.1 (17B100)**. These fields identify the old native host;
  they do not bind it to the current source. Its original build source/date is
  unverified. Current JavaScript was served by the run's owned Metro process.

## Real Execution

The existing press plan's iOS `marketing-screenshots` invocation was launched via
`nativeArgs`, after `checkStorage()`, with a 480-second outer SIGTERM deadline:

```sh
bun app/e2e/cli.ts run --suite marketing-screenshots --driver sim \
  --evidence screenshots --no-record --i-approve-destructive-reset
```

`session-1.json` records a fresh ephemeral iPhone 17 Pro, iOS **26.2**, UDID
`1F076037-4467-4226-AC2D-B7048AD78459`, seed export disabled, owned Metro and
bridge, no video. The runner copied the old native bundle into this new device.
No existing simulator was booted, reset, uninstalled, or used as a test target.
No user profile was read/copied. No funding, payment, posting, or publishing
action was requested.

Onboarding with the new copy passed. Mock Mode and mint selection passed. All 12
named core-tour screenshots completed: wallet, mint-select, mint-info, receive-qr,
send, contacts, dm-chat, feed, notifications, ai, ai model picker, and settings.
The outer deadline then interrupted **T47 goHome**, before verify/final-state.
This was an operator-imposed deadline, not a diagnosed app failure. Wallpaper,
mint-detail and media scenarios did not start.

The runner skipped device cleanup steps after abort, stopped its owned session,
and reported deletion of the exact created simulator. A subsequent `simctl list`
query for that UDID returned no device. Device disposal is not a passing scenario
cleanup/final-state assertion.

## Visual Review And Rejection

Reviewed the historical QR and the new original PNG at:

`app/e2e/artifacts/run-2026-09-14T23-43-43-765Z-f96526dd/marketing.screenshots/named/receive-qr-004.png`

The new screenshot shows the combined receive QR and customization card without
the historical Onchain/BOLT12/Cashu pill row. The four top-level tabs still exist
in current `ReceiveScreen.tsx`; their presence is not itself stale UI. The new QR
contains a different disposable receive request, not payment/settlement proof.
No seed words or private keys are displayed on the reviewed QR screen. This is
diagnostic review, not publication approval. The other 11 PNGs were not visually
approved. Do not publish raw run directories or accessibility/log/state sidecars.

Explicit importer invocation rejected this run with:

> Import requires a complete passing run without failed, skipped, deferred or funded evidence

No `runDir/press/` candidate directory was created. The retained `ios/receive-qr`
entry is marked `availability: unavailable`, `freshness: stale`, with a reason.
Its key, original PNG, run attribution and hash remain intact. No fixed site page
scene referenced this key at inspection. Site geometry/gallery files and store
pins were not edited; site builds/exports were not run.

## Blockers And Retry

Initial free space was about **14 GiB** on the shared checkout/temp/home volume.
After the run it was **8.6 GiB**. The real `press/run.ts ios` retry failed before
Metro at the **10 GiB** gate. Nothing was deleted to make space. The absent native
project/DerivedData means a fresh iOS build would be a cold build; it was not
started below even the capture reserve. No dependencies were upgraded and no
prebuild/clean/build script was invoked.

The focused CLI added here avoids rerunning unrelated tours:

```sh
bun app/e2e/press/run.ts ios --scenario marketing.screenshots --plan
# After adequate storage and a verifiably fresh development build are available:
bun app/e2e/press/run.ts ios --scenario marketing.screenshots
```

Allow enough time for setup, roughly 12 captures, verification and cleanup; the
eight-minute deadline was too short. Artifact presence/version/hash alone still
does not prove build freshness. Promotion remains blocked on a successful native
build bound to source, an entirely passing run, and screenshot/privacy review.
Do not add an unchecked promotion switch to bypass these requirements.

The receive-tabs scenario also referenced the removed method-pill IDs and
`BOLT 12, included` label. Those waits now use the existing in-sheet Unified state
probe and the Advanced BOLT 12 switch's enabled/value state. Screenshot occurrences
(including P2PK occurrence 6) are unchanged. This is a source-confirmed scenario
repair with regression coverage, not a successful native run of that scenario.

## Verification

- JSON validation passed.
- Marketing dry-run: four scenarios ready, no deferrals.
- Fake-driver marketing smoke: four passed; orchestration only, not native proof.
- Initial press plus disposable-session tests: 60 passed.
- Final press, disposable-session and loader regression tests: **112 passed**.
- Repaired receive-tabs scenario: validation, dry-run and fake-driver smoke passed;
  the fake run is orchestration only and cannot supply press images.
- Both iOS and Android TypeScript checks passed after correcting test assertion types.
- Focused ESLint passed; no product code or native driver implementation changed.
- Focused capture/intake regression tests cover exact allowlist selection,
  complete focused intake, truncated-suite rejection and interrupted-run rejection.
- Historical registry provenance remains pinned; explicit stale-status test added.
- Real native outcome: partial capture, interrupted, correctly rejected by intake.
- Full E2E test run before the receive-scenario repair: 801 passed, 2 skipped,
  2 failed. Both failures are untouched viewer matrix count pins at
  `app/e2e/viewer/lib/matrix.test.ts:31` (129 expected, 141 actual) and `:94`
  (90 expected, 104 actual). No scenario inventory/capability changes were made
  by this task; those unrelated count pins were left untouched.
- Android capture, fresh native build, successful complete native rerun and
  promotion were not completed.
